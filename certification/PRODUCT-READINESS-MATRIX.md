# NEUROPAUSE DESKTOP — PRODUCT READINESS MATRIX

**Date:** 2026-08-14 · **Audit basis:** commit `33b9173` (+ round-33 working tree) · **Version:** 1.0.0-rc.18
**Method:** nine parallel read-only domain audits over the actual repository (every claim carries file:line evidence in the per-domain reports), plus executed test suites, a live macOS launch, and a from-commit Windows build. Nothing below is inferred from documentation alone.

**Legend:** GREEN = implemented + tested + verified · YELLOW = implemented, verification gaps · RED = broken/incomplete · GRAY = intentional future scope.

---

## A. THE 27 GATES

| # | Gate | Status | Evidence / dominant finding |
|---|---|---|---|
| 1 | Bootstrap | **GREEN** (was RED→YELLOW; rounds 36/48) | Boot ordering + gates GREEN (`round17CompositionOrder.test.ts`). **Round 36 closed the two RED drivers**: (1) the sticky boot-window race — a `system:runtimeState` channel on the BASE router (registers before the window; live boot log shows base count 19→20 ahead of `window-created` at t+124ms) plus a `system:runtimeStateChanged` broadcast on starting→ready/failed; `AppShell` records a boot-window `xp:profile.get` failure and re-runs the load once on the ready broadcast, so the wrong "skipped" profile is now transient instead of session-long (`AppShell.tsx`, `runtimeReadiness.ts`); (2) silent composition failure — the bootstrap catch marks `failed` with a sanitized first-line message (never a stack, capped; `safeInitFailureMessage` tests) and `RuntimeFailureNotice` renders a `role=alert` naming the failure and the action, instead of a complete UI over ~650 dead channels. Evidence: `runtimeReadiness.test.ts` (8), `ui-tests/runtimeFailureNotice.test.tsx` (4, incl. absence-never-invention), full suite 788/8,197 green, live macOS boot clean (343ms, `Startup complete`, ready broadcast path executed). **Round 48 closed all three remaining residuals — Gate 1 GREEN.** (1) **The ready-window retry is GENERAL**: a live fresh-profile boot (round-46 evidence) showed ELEVEN channels beyond the two taught retriers failing once at first paint (`notifications:list`, `voice:status`, `xp:profile.get`, `livesync:status`, `intent:board/workspaces/governance`, `update:getStatus`, `onboarding:status`, `org:list`, `flags:get`). The fix lives at the ONE place every call passes (`invoke` in `lib/ipc.ts`): a "No handler registered" rejection — which means the handler NEVER RAN, so a retry is safe for reads and mutations alike — waits for the runtime-ready signal (the round-36 broadcast + an immediate base-router state query closing the missed-event race) and retries ONCE; a FAILED runtime, a timeout, a genuinely-missing channel, and every non-boot error surface honestly with no retry (fail-closed untouched). All eleven channels — and every future channel — are covered without touching a component. `ui-tests/bootWindowRetry.test.tsx` (7 — happy retry, missed-event, failed-runtime, timeout, no-loop, denial-pass-through, cached signal) + `assistantBootRace.test.tsx` updated (the chokepoint absorbs the race: no banner ever shown, history arrives on ready, exactly one retry); **negative control: chokepoint neutered → 5 fail; restored → 9 pass.** (2) **Boot-window crash capture exists**: `process.on('uncaughtException'/'unhandledRejection')` at the top of `index.ts` — LOCAL-ONLY logging (crash-report consent untouched), the exception path quits via `app.quit()` so the Gate-16 flush barrier still drains (never a hard `app.exit`), latch against quit loops; rejections logged, never fatal. (3) **The bootstrap finally has its source-scan test**: `bootstrapPins.test.ts` (8) pins the single-instance lock, the deliberate window-first ordering, BOTH runtime-transition broadcasts, failure-never-quits-the-window, the will-quit barrier shape, log-sink-before-bootstrap, the crash handlers, and window-open hardening. Full UI 47 files/313 (every pre-existing renderer test green under the chokepoint) + full main 903/9440; typecheck node+web + lint clean. Round 33 fixed: second-instance crash, tray `app.exit` flush bypass, window-created metric. |
| 2 | Auth/identity | **YELLOW** (was RED) | OAuth/PKCE/loopback + vault GREEN. Round 33 fixed the 3 HIGHs: offline refresh deleted the vault; boot-retry re-sent a consumed refresh token (revokes all the user's sessions server-side); no single-flight refresh. First `authService.test.ts` added (4 tests). Remaining: no re-restore on reachability recovery; renderer auth state machine untested. |
| 3 | Organization/tenancy | **YELLOW** | Resolver chain + refusal vocabulary + W-10 diagnostics GREEN (live-verified on macOS: `LOST`/`RECOVERED` brackets in app.log). Round 33 extended the O-11 fail-closed predicate to `canSwitchTo`/`resolveActor`/`memberIn`. Both former HIGHs **closed by round 40 (Gate 27)**: guards now key on the target org's recorded `ownerUserId` and provisioned spec roles are built-in — see Gate 27 and `provisionedOwnerProtection.test.ts`. |
| 4 | Workspace | **GREEN** (was YELLOW; round 43 verified) | Store + switching + orphan refusal GREEN. The **split-brain switcher HIGH is CLOSED** (fixed round 39, re-verified round 43): the single `WorkspaceSwitcher.tsx` leads with organization workspaces from `enterprise:workspace.list`, switches through the membership-gated `enterprise:workspace.switch` (refusals shown verbatim, `role=alert`), relabels the device-local tab-sets "Views on this device", implements ⌘1–9, and the mojibake is repaired — pinned by `workspaceSwitcherGate26.test.tsx` (6). No open HIGH remains. |
| 5 | Enterprise | **GREEN** (was RED→YELLOW; rounds 36/44/45) | Handlers/stores strong (round 33 fixed the CRITICAL audit-trail reseed). **Round 36 closed all four RED drivers**: (1) Administration has a real error state — every load names itself, a `role=alert` banner lists the failed panels with Retry, and the audit cell renders an explicit error instead of presenting a denied `governance:read` as a clean trail (`administrationErrors.test.tsx`, 4 mounted tests over the real harness incl. retry re-invocation and the honest genuine-empty case); (2) people CRUD is reachable — Enterprise → Customize → People adds/edits/removes members through the audited handlers, owner row protected in the UI (no remove, no email edit per O-13), refusals surface as alerts (`customizePeople.test.tsx`, 4); (3) a throwing license validator answers `'unknown'` (bands `watch`, never green) via the pure `resolveLicenseState` (`commercialModel.test.ts` +4); (4) nav prefs store the HIDDEN set scoped to the managing surface, so a save from the 8-tab Customize can never hide the other 8 Enterprise tabs; the legacy lossy key is discarded so affected installs self-heal (`enterpriseNavPrefs.test.tsx`, 5 — incl. the exact reproduction). Evidence: full suite 788/8,201 + UI 18/155 green; live macOS boot of the built bundles clean (315ms). **Round 44 closed B-1**: `EnterpriseModulesList` now filters by each module's OWN read permission — `registry.readableSummaries(authorize)` omits a module the caller cannot read, so its record COUNT ("how many employees", "which systems") is no longer disclosed to a holder of the coarse `operations:read` (the disclosure `dp:exportable` closed for the export list). `dp:module.list`/`get` already gated per module; the LIST now matches. `moduleRegistry.test.ts` +2 (omits an unreadable module; lists it once the permission is held), commit `<pending>`. **Round 45 closed E-11** — the last named silent-mutation cluster: `DecisionCenterPanel.act`/`onDelegate` (approve/reject/request-changes/escalate/delegate on GOVERNED proposals) were `try/finally` with no catch — a reviewer whose Approve was denied saw nothing and walked away believing the governed action was approved; `CustomizePanel`'s `addUnit`/`addRole` (no catch) and `deleteUnit`/`setChain`/`setRule` (fire-and-forget `void`) failed the same way on the surface that decides which approval chains and compliance rules are ENFORCED. All now surface the refusal verbatim (`role=alert`, "This decision was NOT recorded — …"), the decision card stays open for retry, toggles stay truth-derived (provider updates state only on success, never optimistic). `ui-tests/enterpriseGovernanceErrors.test.tsx` (8 — denied approve says so + card stays open; success closes with no alert; retry clears; denied unit-delete/role-create/chain-toggle/rule-toggle each surfaced; success paths alert-free), negative control 6-fail-neutered → 8-pass-restored, commit `<round-45>`. **Gate 5 now GREEN** — every named driver (rounds 33/36/40 + B-1 round 44 + E-11 round 45) is closed; remaining notes: interactive People click-through beyond mounted tests, and the companion module-families surface (`companion/index.ts` `deps.modules.summaries()`) still uses the trusted unfiltered view — a separate mobile-surface follow-up. |
| 6 | Business | **GREEN** (was YELLOW; rounds 43/47/49) | `BusinessView` is the reference implementation (denial-vs-fault + retry); FamilyDashboard fixed round 36; the module-screen HIGH closed round 43 (`bf1302e`); the Gate-15 medium-tier sweep — Gate 6's last stated YELLOW reason — closed round 47 (`7dcaca6`). **Round 49 verified rather than assumed**, driven by a LIVE report ("business workspace blank" on a refused-tenant profile), and found + fixed one real corner the round-44 B-1 filter had created: with modules registered but NONE readable, `EnterpriseModulesList` returned `[]` and the Business workspace rendered "No business areas yet — nothing is shown that has no real backing" — a permissions problem disguised as an empty install. `readableSummaries` now REFUSES with a named permission message when the registry is non-empty but fully filtered (a genuinely empty registry still returns `[]` honestly), and `BusinessError` classifies it as a denial (lock, no useless retry). Pinned: `moduleRegistry.test.ts` +2 (all-filtered refusal — **negative control: throw neutered → fails**; empty-registry `[]`), `ui-tests/businessGate6.test.tsx` (5 — refused tenant → denial state never blank; transient fault → working "Try again"; all-filtered → denial never "No business areas yet"; genuine empty honest; readable control). Full main 903/9442 + UI 48/318; typecheck node+web + lint clean. The live "blank" report's fully-refused-tenant case renders the denial card (channel-level `operations:read` refusal → `BusinessError`), verified by test 1. |
| 7 | Data | **GREEN** (was YELLOW; round 43) | Import/export engines + SoD + 218 passing tests GREEN; dp:history root cause (W-1) confirmed fixed and wired. `dp:provenance` disclosure **CLOSED** (2026-08-30, `GATE7-PROVENANCE-DISCLOSURE-EVIDENCE.md`): handler now two-gates (`data:read` + module `permissions.read`, restricted values need `permissions.write`) and returns a mapped, redacted DTO via the shared `classifyField` — sensitive `fields[].original`/`transformation` redacted, and `tenantId`/`workspaceId`/connector internals no longer cross IPC (`dataPlane/index.ts` `redactProvenance` + handler; `provenanceDisclosure.test.ts` 6/6). **Round 43 closed both remaining HIGHs** (`GATE7`… evidence + commit `9082f56`): (1) provenance write failure was swallowed — `ProvenanceStore.persist()` returned a `.catch(()=>undefined)` promise, so `append()` resolved even when the disk write threw and an import reported success with the trail only in memory; `persist()` now rejects for the current caller while a separate swallow keeps one failed write from poisoning the next (`provenanceWriteFailure.test.ts`, 3). (2) update-only runs rendered "0 records imported" — `buildOverview` counted only `totals.imported`; a "Records updated" metric, headline clause and per-run count were added (`dataCommandCenterModel.test.ts` +2). **Gate 7 now GREEN.** |
| 8 | AI/Assistant | **YELLOW** (was RED; round 34) | Assistant pipeline, fallback chain, worker execution GREEN. **Round 34 fixed the promise defect**: the tenant preference now clamps the route plan (`min(platform,tenant)` inside `assembleRouteCandidates`, reconfigure on preference change + workspace switch, display/router mode unified) — pinned by new routing tests including the exact reported failure. OpenAI added on the shared ModelClient contract (own vault entry, 401/429 naming, candidate ordering, client tests). Ollama detection distinguishes installed vs running vs models; consent-first model pull; provider Settings + first-run made actionable; the org preference is finally visible/settable in Settings. **D-4 closed (round 35, GREEN)**: the boot router is now fail-closed — `createBootRouter()` (never configured; `complete()` rejects) replaces the bare env-keyed cloud client at engine construction, so a boot-window request takes the engine's existing deterministic-fallback path (provenance `'none'`, nothing leaves the machine, no invented tenant/policy), and a failed reconfigure now parks fail-closed instead of retaining policy-blind external routing. Root cause: initialization-order with a fail-open placeholder (`engineInstance.ts:12` built on `createModelRouter()` at module import; `runtimeCore.ts:747` upgrades fire-and-forget). Evidence: `bootWindowRouter.test.ts` (4 tests — unconfigured even with env keys present; deterministic fallback with `'none'` provenance; direct `complete()` rejects with the initializing message; `setRouter` swap restores normal routing on the same instance) + full AI/tenancy/IPC regression (109 files / 1,496 tests) green. **Live-provider item closed for local, wire-proven for cloud (round 35)**: (1) LIVE — a real inference through the full production chain (ai-config → Vault → tenant clamp set to `local_only` via the real store → planRoute → PrivateFirstClient → running Ollama, model `llama3.1`) completed in 1.7s with execution-stamped provenance `{location:'local', provider:'ollama', mode:'local_only'}` and real token counts — `liveProviderVerification.test.ts`, run with `NP_LIVE_AI=1` on 2026-08-14 (env-gated; skips, never fakes, without the flag). (2) DETERMINISTIC WIRE — `providerWireIntegration.test.ts` (7 tests, always-on): real localhost HTTP servers speaking the exact Anthropic/OpenAI/Ollama protocols driven through the same full chain via a `fetchImpl` origin-rewrite seam; proves at a real socket that `local_only` with both cloud keys in the vault produces ZERO cloud requests, the tenant clamp holds at the wire, private_first falls back local→cloud only WITH consent and names the failed local attempt, refuses without consent, and a 401 surfaces its reason with the key never present in the response. REMAINING (cloud-live): Anthropic/OpenAI live completions require real API keys — the gated tests exist and skipped; run `NP_LIVE_AI=1 ANTHROPIC_API_KEY=… OPENAI_API_KEY=… npx vitest run src/main/ai/liveProviderVerification.test.ts` when keys are available. |
| 9 | Connectivity | **GREEN** | Best-audited domain: OAuth, vault, coalesced refresh, reauth marking, health, permission gating, zero secrets in logs (grep-verified). Round 43: the "no logger redaction layer" residual is **RESOLVED** — `logger.ts` enforces credential redaction at the console+file boundary (`redactCredentialText` + `classifyFieldName`, NP-013). Remaining residual (non-blocking tech-debt): Slack Socket Mode dead code + unused `ws` dep (also Gate 25) — left in place; removing a shipped dependency is out of scope for a readiness pass. |
| 10 | Security | **YELLOW** | IPC pipeline (sender trust → auth → permission → Zod) GREEN; owner/role/tenant guards GREEN for the seeded org. Open: Gate-3 provisioned-org HIGHs; `workspace-ctx:*` channels unauthenticated by design but outside the classification invariant (`router.ts:136-150`); cloud org channels authorize on membership only (viewer can invite/remove — enforcement claimed server-side, unverified); dp:provenance disclosure (Gate 7). |
| 11 | Database | **GREEN** (was RED→YELLOW; round 43) | ~85 hand-rolled JSON stores, atomic writes; quarantine-not-reseed on the org/workspace/audit stores (round 33). **All remaining Gate-11 items are now closed:** (a) connector/secret vaults quarantine-not-reset AND are inside backup — `connectorVault.ts`/`secureStore.ts` + `storePaths.ts`, commit `9bb49b9`; (b) backup-registry completeness — ~27 customer/tenant/audit/config stores (decision-records, holds, data-plane-*, action-records, m365-governed-actions, erp-*, medical-device-traceability, documents/, sandbox/, …) added to `DOMAIN_FILES`, raw signing-secret files deliberately excluded (`storePaths.test.ts` +1), commit `dcee913`; (c) migration ordering — a transforming startup migration on an already-versioned install now relaunches so stores reload the migrated bytes (`shouldRelaunchAfterMigration` + `runStartupMigrations`, fresh install exempt, `migrationEngine.test.ts` +5), commit `dcee913`. |
| 12 | UI/UX | **YELLOW** (was RED; round 36) | 55 sections, zero dead routes, real design system. **Round 36 closed the four RED drivers at their roots**: (1) the accent token is now a COLOR (`--accent: rgb(var(--accent-ch))`; channels moved to `--*-ch` for Tailwind/alpha consumers) so all 30+ raw `var(--accent)` color consumers — the transparent Intent Home CTA, invisible active-tab underlines, vanished rings/chart bars — are valid by construction; `cssTokens.test.ts` gained TYPE checks (+3, incl. a double-wrap guard) and the built CSS carries the new tokens (37 occurrences); (2) a real `useFocusTrap` (Tab/Shift-Tab cycling, focus restore) applied to the shared `Modal` primitive and the destructive-delete alertdialog, which also gained Escape-cancels and `aria-modal` (`focusTrap.test.tsx`, 3); the harness motion mock now forwards refs; (3) the renderer knows its platform (`lib/platform.ts`) — the Toolbar's 80px traffic-light gutter is macOS-only; dead `TitleBar.tsx` (same bug, 0 importers) removed; (4) the three worst failure-as-emptiness screens fixed with error+retry states that keep genuine-empty honest: AI Store search ("No matching apps" no longer shown over an outage), Getting Started (no eternal spinner / fabricated "0 of 0 done"), enterprise search ("try a different term" no longer blames the user for a backend fault) — `errorStatesGate12.test.tsx` (4, incl. retry-then-honest-empty). Evidence: full suite 788/8,204 + UI 20/162 green; live macOS boot of the built bundles clean (335ms). **Round 50 closed the focus-trap cluster — the last code-resolvable Gate-12 item.** The five hand-rolled overlays that announced `aria-modal` while Tab walked into the shell now use the round-36 `useFocusTrap`: (1) the first-run takeover (`FirstRunExperience`) — trapped for its whole life; Escape deliberately does NOT dismiss the required, consent-bearing flow (it has an explicit Skip); (2) the onboarding wizard (`OnboardingWizard`) — trapped, and Escape takes the RECORDED dismiss path (the same persisted `onboarding:dismiss` the "Skip tour" button uses — never a silent vanish); (3) the sandbox inspector `Drawer` — trapped, Escape closes (parity with its backdrop click); (4) the AI-Store `InstallFlow` — which had never declared `role="dialog"` at all: now `role="dialog"` + `aria-modal` + `aria-label`, trapped, Escape closes (backdrop parity); (5) the Developer Portal `Modal` — trapped (its Escape already existed), focus restored to the opener on close. `ui-tests/focusTrapGate12.test.tsx` (6 — each overlay's containment via the walk-N-Tabs negative "focus cannot escape", Escape paths incl. the wizard's persisted dismiss and the first-run non-dismissal, dev-modal focus restore); **negative control: `useFocusTrap` no-opped → 4 fail; restored → 6 pass.** Full UI 49 files/324 (all pre-existing overlay flows green under the traps) + full main 903/9442; typecheck web + lint clean. The error-hiding sites listed here closed in rounds 43/47 (Gates 6/15). Remaining YELLOW (none code-resolvable): preview-tier placement of Enterprise/Marketplace nav is a product decision left open; Windows-only `backdrop-filter` cost + Segoe type metrics and the Windows visual pass are machine-blocked (Gate 20's hold). |
| 13 | Onboarding | **YELLOW** (was RED; round 36) | **Round 36 closed the five RED conditions**: (1) the flow RESUMES — the pure `resumeStep(profile)` derives the step from persisted milestones (aiModeChosen → workspace, workspaceType → discovery) and the component honors it, with `chosenType` rehydrated, making the service's round-17 resume promise true; (2) "Sign In" is a DETOUR, not a forfeit — the terminal `state:'skipped'` write is gone; the takeover hides for the Settings visit and returns (at the resumed step) when the user leaves; (3) the final "Yes, continue" surfaces a failed completion write; (4) `skip()` fires `onDone` only when the persist SUCCEEDED — a failed skip is said, and the flow no longer pretends to close; (5) every step after welcome has Back. The shared error banner moved to the flow container (it used to exist inside only 2 of 5 steps, so welcome/understanding failures set state that rendered nowhere). Evidence: `firstRunResume.test.tsx` (5 — resume rule + resumed mount + back + completion-failure alert + skip-failure with onDone NOT called) over the REAL profile service; all 8 pre-existing first-run tests still green; full suite 788/8,204 + UI 21/167; live macOS boot clean (356ms). Remaining YELLOW: the SECOND onboarding system (`OnboardingWizard` checklist, its own silent failures/no Escape/no focus trap) unchanged — the two-systems-back-to-back design itself is a product decision left open; the Sign-In detour's return-on-leave-Settings is session-local (relaunch resumes correctly regardless); no automated AppShell-level test of the detour (AppShell has no mounted harness). |
| 14 | Navigation | **GREEN** | All 55 section ids have render cases (diffed registry↔switch); deep links sound; nav locks pinned by 39 tests. LOW: no registry↔switch parity test; `default:` falls back to the retired home view. |
| 15 | Error handling | **GREEN** (was RED→YELLOW; rounds 36/43/47) | Main process was already disciplined (0 empty catches; 94.5% of 292 catch-defaults verified correct). **All six named RED clusters are closed**: Administration (Gate 5), enterprise search + AI Store (Gate 12), and in this pass — (1) the `org.list().catch(()=>[])` + `orgs[0]` idiom replaced by the shared `fetchActiveCloudOrg` resolver (throws on failure; FINDING-6 name-match; null-on-ambiguity with the shared message) across FeatureFlagsCenter (a failed license read no longer shows free-tier entitlements to a paying customer), SubscriptionCenter (an outage no longer offers the create-your-first-org wizard), TrustedDevices (security surface; a failed self-registration is also now said via a status note), and EnterpriseOverview (FINDING-6 fixed here too; device-read failures throw instead of rendering "0 devices"); PerfSampler keeps the idiom deliberately (best-effort telemetry); (2) family dashboards: a failed/denied module read renders a NAMED error with retry instead of "No records yet — this dashboard draws every number from live records"; (3) assistant: a failed conversation-list read and failed pick/pin/delete are said through the live-note channel instead of silent no-ops; (4) main process: `assistantService.safeRecall` — the one exception to the file's own unavailable-reporting contract — now reports `{system:'memory'}` into the envelope. Evidence: `errorStatesGate15.test.tsx` (8 — resolver truth table incl. throws-on-down and name-match; flags error-not-free-tier ×2 with retry re-invocation; dashboard named-error + honest-empty pair) + `assistantService.test.ts` +1 (memory unavailable); full suite 788/8,205 + UI 22/175 green; live macOS boot clean (344ms). Remaining YELLOW: the sweep's medium tier — `enterpriseIntelligenceSubsystem` safe()/safeEvents(), `insight` dashboard's one bare healthHistory catch, `crashReporter.export→[]` (support surface), `searchv2` memory hits, `KnowledgeWorkspaceView` null-as-no-results, `decisions/instances` linkReader, Sync/Knowledge panels, `WorkforceProvider.installs`, `IntegrationHealthPanel`, Settings crash-consent revert, ExecutePanel. **Round 43 closed the module-screen refresh/submit catches** (Gate 6 above; `bf1302e`). **Round 47 closed the FIVE remaining named sweep sites — Gate 15 GREEN:** (1) `KnowledgeWorkspaceView` — `settled()` now records WHICH sources failed and a `role=alert` names them with Retry (partial degradation kept; one failed source no longer renders as an all-clear of empty tiles), and a FAILED federated search is its own state with Retry, never "No matches"; (2) `IntegrationHealthPanel` — a failed sync-state read shows its reason + Retry instead of "No active integration syncs yet" (a live `onSyncState` broadcast clears it); (3) `ExecutePanel` — a denied target list is named (never "No <mode>s available" over a refusal) and the sessions/history poll's failures raise a sticky banner that self-clears on the first successful poll; (4) `WorkforceProvider` — `installs().catch(()=>[])` now records `installsError` in context (partial degradation kept; exposed for surfaces — no current surface lists installs, noted); (5) Settings crash-consent — a refused consent write reverts AND says why ("the previous setting is still in effect"), consent state always derived from the backend's answer. `ui-tests/errorStatesGate15Round47.test.tsx` (12 — each failure surfaced + each genuine-empty/success honest + Retry recovery), **negative control executed: fixes neutered → 7 fail; restored → 12 pass.** UI 46 files/306; full main 902/9432; typecheck web + lint clean. Remaining (documented, non-blocking): the backend-tier swallow-to-empty helpers (`enterpriseIntelligenceSubsystem.safe()`, `crashReporter.export→[]`, decisions `linkReader` — main-process diagnostics, not UI lies); the sweep's UI tier is CLOSED. |
| 16 | Recovery | **GREEN** (was RED→YELLOW; rounds 37/46) | Session/workspace restart restoration was already GREEN; interrupted-startup silence was closed by Gate 1 (round 36, `RuntimeFailureNotice`). **Round 37 closed the two remaining REDs**: (1) a shutdown flush BARRIER — dependency-free registry (`shutdownFlush.ts`, same shape as `workspaceSwitchHub`) + a `will-quit` handler that defers the quit, drains every registered flush time-boxed and failure-isolated, and logs the summary; registered: app-log, workspace-contexts (migrated off its private hook), platform-timeline (`platform.dispose()` finally has a caller via `platformDisposeRef`), org/workspace/governance stores, and ONE entry draining all ~106 module stores (`registry.flushAll()`); **live-verified**: a real graceful quit logged `Shutdown flush complete {ran:7, failed:[], timedOut:[]}`; (2) install-wide restore — `RestoreResult.requiresRestart` (true on success, false on refusal), the copy loop is atomic per file (tmp+rename; no torn store files for the quarantine machinery to eat), and the `backup:restore` / `recovery:run` IPC handlers ENFORCE the relaunch (delayed so the renderer sees the result; `app.quit` not `exit`), with `suppressShutdownFlushOnce` so the barrier cannot persist stale pre-restore memory over the restored bytes — the two fixes were designed not to undo each other, and the one-shot semantics are pinned by test. Evidence: `shutdownFlush.test.ts` (5 — full drain, thrower isolation+naming, hang time-box, replace-not-duplicate, one-shot suppression) + `backupManager.test.ts` +3 (requiresRestart true/false, no tmp residue, restored bytes verified); full suite 789/8,213 green; live quit verified. **Round 46 closed the two named residuals — Gate 16 GREEN.** (1) **Full barrier coverage**: an independent lifecycle sweep found **40 fully-uncovered coalesced-write stores + 1 partial** (graph, memory+audit, webhooks, workforce ×4, ecosystem ×7, cloud ×3, federation ×5, sandbox ×7, decisions/holds, documents, identity, ERP approvals, medical-device traces, opportunity/outcome, personalization, marketplace policy, ai-routing-usage) — every one loses its in-flight write on a raced quit. **16 new registrations** at the composition points that already import the instances (one per subsystem, the round-37 `enterprise-module-stores` idiom; zero new coupling), locked by `shutdownFlushCoverage.test.ts` (24 — source-scan per registration name+file, limits stated in-file; **15 failed before the fix**, load-bearing by construction). (2) **Suspend flush**: `powerMonitor('suspend')` now drains the same barrier via `runSuspendFlush` — a lid close never fires `will-quit`, so a battery death while asleep lost everything pending; the suspend drain RESPECTS a pending restore-relaunch suppression WITHOUT consuming it (the one-shot flag still belongs to the quit it was armed for — pinned), same failure isolation + time-box (`shutdownFlush.test.ts` +3). Full main 902 files/9432 passed; typecheck+lint clean. **LIVE-VERIFIED (2026-08-30):** a real macOS run of the round-46 build (alternate outDir `out-flush-check` — armed `out/` byte-untouched; fresh temp profile) booted clean (`Startup complete 478ms`, 723 secure handlers) and a graceful quit logged **`Shutdown flush complete {"ran":23,"failed":[],"timedOut":[],"durationMs":1}`** — up from the round-37 `ran:7`, every registered subsystem drained, none failed or timed out. The same run also live-confirmed Gate 11's migration ordering on a fresh install (`from:0→2`, baseline + schemaVersion stamped on 17 stores, correctly NO relaunch for fromVersion 0). Remaining (unchanged, documented): the relaunch enforcement has no automated test (needs Electron app lifecycle — verified by code + tested seams both sides). |
| 17 | Test suite | **GREEN** | Executed at round-33 tree: **781+3 main files / 8,155+13 tests + 14 UI files / 138 tests — all pass**; typecheck node+web clean; lint clean; test-config baseline 61 pre-existing errors (unchanged). Coverage shape: main process deep, **renderer components ~zero** (14 ui-test files; none mount Enterprise/Business/Administration/Organization/AppShell). |
| 18 | E2E product test | **YELLOW** (was RED; round 38) | **The journey now exists, is automated, and passed — twice over: once through the real main-process classes, once on the real app.** (1) `src/main/e2e/productJourney.test.ts` (8 phases, 8/8 passing) drives one continuous product story over the REAL production classes — no jsdom, no handler mocks; only Electron's `app.getPath`/`safeStorage` are stubbed onto a temp dir: fresh install seeds honestly (owner email null, `workspace-default` active) → onboarding persists the privacy choice (`local_only`) and the profile RESUMES at `discovery` (Gate 13's `resumeStep` verified in-journey) → the privacy promise holds at the routing layer (Claude configured + env key present, yet `assembleRouteCandidates` clamps to `local_only` — zero external candidates in the plan) → owner claim (first-claim-wins) + real person + new 'Operations' workspace admitted through the REAL membership gate (`createTenantContextResolver().canSwitchTo`) → CRM record in the tenant-scoped module store → the full data pipeline: analyze → import (real XLSX) → history shows the run → audit trail records it → export writes a real file (`records>0, cancelled:false`) → the boot-window AI engine fails CLOSED (model `'none'`, provenance `location/provider 'none'` — nothing invented, nothing external) → then the world is flushed, REBUILT from disk, and every fact re-asserted: profile completed, `local_only` retained, owner + person + 'Operations' + both CRM records + import history + audit entries persist, and `verifyAuditIntegrity().ok` proves the hash chain survived the restart. (2) REAL-APP evidence (2026-08-14, dev profile preserved and restored): on a genuinely fresh profile, first boot seeded `orgs:1 … users:1` with honest `not_signed_in` refusals; graceful quit logged `Shutdown flush complete {ran:7, failed:[], timedOut:[]}`; relaunch loaded `users:28` (seed + the 27 AI-workforce rows persisted through the round-37 barrier) with `workspace-default` still active — fresh-install → quit → restart persistence demonstrated on the actual Electron app, log preserved. Full regression at this tree: main 790 files / 8,221 tests + UI 22 / 175 + typecheck + lint all green. Remaining YELLOW: the renderer layer is exercised only via the jsdom harness (Gates 12/13/15 tests), not by a driven-UI click-through of the packaged app; Windows end-to-end remains machine-blocked (Gate 20). |
| 19 | macOS | **YELLOW** | Live launch verified at this commit: full subsystem bring-up, 19+721 IPC handlers, tenant `not_loaded`→`RECOVERED` in 24ms on second boot, no new errors across restart. Known boot-window artifacts (first-boot graph/memory rebuild ordering, identity first-run ERROR each boot). Full workflow click-through not automated. |
| 20 | Windows | **YELLOW** (was RED-BLOCKED; round 42 — real Windows runtime achieved) | **The machine block is resolved: rc.20 ran on real Windows 11 ARM64 and the core boot-health / IPC / tenant / persistence matrix PASSED.** Built from tag `v1.0.0-rc.20` (`efe8196`, clean): `NeuroPause-Setup.exe` SHA-256 `e861228f2af873a8d67036fbb0407a6e8acab07f3bb5b6f009815f824ac8bc90`, `verify:release --platform win` 6/6 PASS. Ran it on **Windows 11 Pro ARM64 (10.0.26100.1, 24H2)** — a QEMU/HVF guest on this Apple-Silicon Mac (x64 NSIS binary under Windows' inbox x64 emulation; caveat recorded). **Verified ON the Windows machine**: installer `sha256.match=True` against the expected rc.20 hash; embedded build-info `version=1.0.0-rc.20, commit=efe8196, dirty=False`. Runtime evidence from `%APPDATA%\@neuropause\desktop\logs\app.log` across 7 real launches — `Startup complete … complete:true` (window-created + runtime-core-ready, ~1.3s); **`Secure IPC handlers registered {"count":722}`**; `Organization runtime ready {orgs:1,units:13,roles:6,users:1}` (fresh seed) + `Workspace manager ready {active:"workspace-default"}`; `AI engine configured {provider:claude, configured:false}`; **repeated launch ×5 → no-handler=0, init-fail=0 every time (no IPC-registration race, the outage class this gate exists for)**; restart persistence `Tenant resolution LOST=0`; W-10 boot-window bracket `tenant.refused=2` then resolves — **no persistent `not_a_member`**. The UI renders on the Windows desktop with the honest no-backend error state ("…Nothing is wrong with this computer. [Retry]"). Evidence bundle + screenshots: `certification/windows-runtime-evidence-rc20/`. **Why YELLOW, not GREEN**: the sign-in-gated section-B items (B3 signed-in tenant context, B5 import/export, B6 Business/Assistant, B9 owner-row spot-checks) need the cloud auth backend, which is unavailable in this offline VM (the app correctly refuses sign-in) — those are macOS-verified on the identical binary + code-present in the Windows `app.asar`, but not driven on Windows here; likewise the graceful `Shutdown flush complete` line (force-kill correctly runs no flush; harness can't drive the app's real quit). GREEN needs a networked Windows run that signs in and drives the interactive flows. |
| 21 | Release engineering | **GREEN** (after fix) | Found + fixed this session: the working tree claimed 1.0.0-rc.17 while tag `v1.0.0-rc.17` = `e09df1e` (two binaries, one version — the exact `da36851` failure). Bumped to **1.0.0-rc.18** with CHANGELOG. Verifier + provenance stamping (commit/dirty in build-info) GREEN. |
| 22 | Performance | **GRAY** | Not measured this session beyond boot timing (window in ~1s; runtime core seconds later — the Gate-1 race). `startupMetrics` now records window-created (round 33). No profiling performed; no verified performance defect to fix. |
| 23 | Data integrity | **YELLOW** (round 43: atomicity closed) | Orphan refusals + cascades GREEN. **Round 43 closed the org-creation atomicity HIGH**: `provisionOrganization` wraps the post-create steps and rolls back on failure via `orgStore.removeProvisionedOrganization` (removes only that org's rows, refuses the seeded org, not IPC-exposed), so a failed second-org provision no longer leaves an un-enterable, un-cleanable tenant — `multiOrgTenancy.test.ts` +3, `orgStore.test.ts` +3, commit `bac4eb2`. Remaining YELLOW (lower severity, deliberately deferred): no org/workspace NAME uniqueness (a product-behavior decision — rejecting duplicate names changes existing flows); pre-P13A provenance rows presented as "No imports yet" (cosmetic copy). |
| 24 | Security regression | **GREEN** (was YELLOW; rounds 40/43/51) | Strong negative suites (round9/round10/crossTenant/multiOrg); provisioned-owner takeover pinned round 40; corrupt-row-through-resolveActor + malformed-payload-at-bridge pinned round 43 (`bac4eb2`). **Round 51 verified the whole landscape** (independent inventory) and closed the gate: all TEN named negative classes fail closed in production and are each pinned over a REAL enforcement path — (a) cross-tenant read `crossTenant.test.ts`; (b) cross-tenant write/takeover `round10OrgOwnership` + `provisionedOwnerProtection`; (c) confused deputy `governedActor` + `s4TenantUnification`; (d) hostile renderer forged authority `router.governedTransport` + `approvalActionBinding` + `constitutionalInvariants` RULE-001/003; (e) prompt injection `capabilities/assistantMailIntent.test.ts` (13 — the corpus phrases, out-of-scope→UNSUPPORTED, hostile-drafter-can't-escape-scope; and the ONLY free-text→action surface is `mail.send`, so cohort capabilities have no injection surface — dispositioned N/A, not a gap); (f) corrupt row `authzGate`; (g) malformed payload `secureBridgeTimeout`; (h) replay/single-use `executeEngine.durableConsumption` + `governedAction.negative`; (i) unauth channel `runtimeAuthz`; (j) AI-as-untrusted `constitutionalInvariants` RULE-001/007/011. **New this round:** `securityRegressionGate24.test.ts` (16) — a coverage MANIFEST that fails if any class loses its pinning test/marker (a legibility invariant the gate lacked), PLUS the one genuinely-missing pin the inventory found: the public execute channel (`connectors:m365.execute`) separates renderer-supplied `confirmed` from main-resolved `actorId`/`tenantId`/`ownsAccount` — Gate-33 at the real handler wiring, complementing `governedAction.negative`'s proof that `confirmed:true` is powerless without those facts (ownsAccount:false + confirmed:true ⇒ DENIED). **Negative controls executed:** renderer-trusted `actorId: r.actorId` ⇒ wiring test fails; a pinned file losing its marker ⇒ manifest fails. Full main 903/9458; typecheck node + lint clean. No open vulnerability in any class. |
| 25 | Feature completeness | **GREEN** (sweep) | 0 TODO/FIXME in product source; every gap is declared, not hidden. 16 real incomplete items catalogued (top: Slack realtime dead code+dep, security capabilities `needs-backend`, 9 preview-only connectors, PDF extraction). |
| 26 | Real user acceptance | **YELLOW** (was RED; round 39) | **The macOS interactive journey was DRIVEN, as a user, on the real app** (Playwright-core driving the repo's own Electron binary on a genuinely fresh profile; dev profile preserved and restored; 25+ screenshots): create account → first-run (privacy step live-detected the running Ollama; "Keep it on this device") → workspace type → discovery → provenance-labeled confirmation ("Inferred — please confirm") → 6-step tour → main shell → Assistant. Of the four source-identified friction points, three were already closed by Gates 13/5 and were CONFIRMED live (onboarding resumed at discovery after a hard kill; admin errors named; people CRUD reachable; owner claim worked — Settings shows "Your role: Owner" for the fresh account). **Three genuine blockers found by driving and fixed at their roots**: (1) THE FLAGSHIP FAILURE — a fresh local-only user with a Connected Ollama got "No AI model" for every session: the engine's boot reconfigure raced tenant resolution (live log: configured:false at T+13ms, RECOVERED at T+19ms) and nothing ever rebuilt it, so the D-1 clamp never saw the preference row. Fix: `tenantRecoveryHub` (workspaceSwitchHub's sibling; announced from the resolver's `onRecovered`, the one place recovery is decided) + engineManager subscribes — recovery is now the third reconfigure trigger next to preference-set and workspace-switch. Live-verified: configured:false → RECOVERED → **configured:true 2ms later**, and the Assistant answered with a real llama3.1 narrative badged **Local** (usage file: `byLocation.local:1`, zero external). (2) THE SPLIT-BRAIN SWITCHER — the sidebar's only switcher drove the untenanted `workspace-ctx` tab-set store under the heading "Workspaces" (with ⌘1–9 hints no handler implemented, mojibake "â€¦" in shipped copy, and a "create" that made a local tab-group no enterprise surface lists). Fix: the popover now LEADS with the organization workspaces from `enterprise:workspace.list`, switches through the membership-gated `enterprise:workspace.switch` with refusals shown verbatim (role=alert), links "Manage members and workspaces in Settings…", relabels the tab-set list "Views on this device"/"New view…", implements ⌘1–9, and repairs the double-encoded UTF-8. (3) THE ASSISTANT BOOT RACE — with Assistant as the restored section, a real relaunch mounted it before secure-IPC registration; "No handler registered for 'assistant:conversations'" was honestly shown but never healed. Fix: AppShell's Gate-1 idiom applied — the failure is recorded as boot-raced and the runtime-ready broadcast re-runs the load once (live-verified: history fully loaded on the next relaunch, no banner). Tests: `engineManager.test.ts` +1 (the ANNOUNCEMENT rebuilds the router — boot inside refused window parks needs-setup, recovery + visible pref → configured), `ui-tests/workspaceSwitcherGate26.test.tsx` (6 — org rows from the real channel, gated switch invoked, refusal verbatim, list-failure honest with views usable, ⌘2 switches/⌘1 no-ops, Settings nav), `ui-tests/assistantBootRace.test.tsx` (2 — retry on ready via the harness's new `emitBroadcast`; no redundant refetch after success). Full regression: main 790 files / 8,222 + UI 24 / 183 + typecheck + lint green; graceful quit logged `Shutdown flush complete {ran:7}`. Remaining YELLOW: Windows acceptance machine-blocked (Gate 20); driven journey covered account→onboarding→AI→settings→switcher→relaunch but not an interactive Data-import or multi-workspace switch click-through (a second org workspace has no creation UI outside Enterprise flows); after a tenant switch other mounted surfaces still refetch only on navigation; general-knowledge questions are deterministically declined by design (grounding discipline) — fine, but the intent screen's example-only guidance may read as a limitation to new users. |
| 27 | Final release gate | **YELLOW** (was RED; round 40) | **Every named release blocker is fixed with evidence or explicitly dispositioned — see §B for the item-by-item verdict: GO for macOS `1.0.0-rc.20`; Windows is a platform HOLD (machine-blocked, procedure ready), not an unknown.** The last un-dispositioned blocker (§B item 4, provisioned-org owner protection) was fixed at the root this round: `Organization.ownerUserId` recorded by provisioning in the act that creates the owner (first-set-wins, seeded org refused, foreign users refused); `guardOwnerUserPatch`/`canDeleteMember` now key on `orgStore.protectedOwnerIdForTarget()` (the target's OWN org's recorded owner; falls back to the seeded literal — never less protection); provisioning creates its spec roles `builtIn:true` so the provisioned Owner role is no longer deletable/de-permissionable; pre-round-40 stores heal on load when the owner is unambiguous (exactly one human titled 'Owner' holding the org's Owner role) and log the ambiguity instead of inventing a root of trust when not; the Customize People panel keys its owner protections on the recorded owner. Release discipline executed at the release commit: version `1.0.0-rc.19`→**`1.0.0-rc.20`** in both manifests (five rounds of fixes had shipped since the rc.19 stamp — the da36851 two-binaries-one-version failure class, caught again by this gate), CHANGELOG rc.20 section covering rounds 35–40, tag `v1.0.0-rc.20` on release commit `efe8196`, macOS artifact packaged FROM that commit (build-info: `commit efe8196, dirty:false`) — DMG SHA-256 `8197ef5ad0f207d57dee76703dc2b880fb5e2179a86c0eb6892e0c5081c92859`, update zip `8e438b73…786d` — with `verify:release` all-PASS (feed v1.0.0-rc.20 ↔ binary sha512/size), and the PACKAGED app boot-verified live: `Startup complete 271ms`, 722 secure handlers, the round-39 recovery reconfigure firing in the shipped binary (`configured:false → RECOVERED → configured:true`), graceful quit `Shutdown flush complete {ran:7}`. Evidence: `provisionedOwnerProtection.test.ts` (8 — anchor recorded; first-set-wins; the Round 9 takeover replayed one tenant over with every step refused; Owner role undeletable; unambiguous legacy heal + honest ambiguous refusal; seeded org byte-for-byte unchanged), enterprise+tenancy 237 files green, full regression + typecheck + lint green. Remaining YELLOW (why not GREEN): Windows acceptance is still a hold — the gate cannot claim a shippable PRODUCT, only a shippable macOS candidate; the YELLOW-tier residuals catalogued across Gates 2–24 are open engineering debt, none release-blocking. |

**Tally (after round 33): GREEN 5 · YELLOW 9 · RED 12 · GRAY 1.**
**Tally (after round 34): GREEN 5 · YELLOW 10 · RED 11 · GRAY 1** — Gate 8 RED→YELLOW (routing promise fixed, tested; runtime completion against live providers still unverified).
**Tally (after round 38): GREEN 5 · YELLOW 18 · RED 3 · GRAY 1** — Gate 18 RED→YELLOW (rounds 35–38 closed Gates 1, 5, 12, 13, 15, 16, 18 in sequence). RED remaining: 20 (Windows, machine-blocked), 26, 27.
**Tally (after round 39): GREEN 5 · YELLOW 19 · RED 2 · GRAY 1** — Gate 26 RED→YELLOW (the macOS journey driven live; boot-race AI blocker, split-brain switcher, and assistant boot race fixed). RED remaining: 20 (Windows, machine-blocked), 27.
**Tally (after round 40): GREEN 5 · YELLOW 20 · RED 1 · GRAY 1** — Gate 27 RED→YELLOW (§B verdict: GO for macOS `1.0.0-rc.20`; provisioned-owner protection fixed; release discipline executed). The ONLY remaining RED is Gate 20 — Windows, machine-blocked with the procedure and artifact pipeline ready.
**Tally (after round 41): GREEN 5 · YELLOW 20 · RED 1 · GRAY 1** — unchanged by design: Gate 20 stays RED (BLOCKED) because no Windows runtime exists in this environment and runtime evidence is the gate's own PASS bar; the rc.20 installer is now built from the exact release tag, hash-recorded, provenance- and marker-verified, and the procedure is re-stamped so the acceptance run can start the moment a Windows machine is available.
**Tally (after round 42): GREEN 5 · YELLOW 21 · RED 0 · GRAY 1** — Gate 20 RED(BLOCKED)→YELLOW: rc.20 was run on real Windows 11 ARM64 (QEMU/HVF on this Mac) and the boot-health/IPC-race/tenant/persistence matrix PASSED with genuine runtime evidence (`certification/windows-runtime-evidence-rc20/`); the sign-in-gated interactive flows remain backend-blocked in the offline VM. **No RED gates remain.**
**Tally (after round 51): GREEN 14 · YELLOW 12 · RED 0 · GRAY 1** — Gate 24 YELLOW→GREEN: an independent inventory confirmed all ten named negative classes fail closed and are each pinned over a real enforcement path (the "14 scenarios" resolve to these ten, with the injection-breadth item dispositioned N/A — one free-text surface, comprehensively pinned); a new coverage manifest makes the set fail-if-dropped, and the one missing pin (public-execute channel separating renderer consent from main-resolved authority) was added. 16 new tests + two executed negative controls; no open vulnerability.
**Tally (after round 50): GREEN 13 · YELLOW 13 · RED 0 · GRAY 1** — Gate 12's focus-trap cluster (its last code-resolvable item) closed: all five hand-rolled overlays trap focus, handle Escape through their real close paths (the wizard's is persisted; the first-run takeover deliberately does not Escape-dismiss), and InstallFlow finally declares `role="dialog"`. 6 new tests + a 4-fail negative control. Gate 12 stays YELLOW only on a product decision (nav placement) and the machine-blocked Windows visual items.
**Tally (after round 49): GREEN 13 · YELLOW 13 · RED 0 · GRAY 1** — Gate 6 YELLOW→GREEN: its last stated residual (the Gate-15 sweep) was already closed in round 47; round 49 verified rather than assumed and fixed the one real corner found (all-filtered module list rendered as "no modules" — now a named permission refusal), with 7 new tests + a negative control.
**Tally (after round 48): GREEN 12 · YELLOW 14 · RED 0 · GRAY 1** — Gate 1 YELLOW→GREEN: the boot/ready-window retry generalized to the invoke chokepoint (11 live-reproduced channels + all future ones covered; 7 race tests + negative control), boot-window crash capture added (local-only, flush-preserving), and the bootstrap gained its source-scan pins (8).
**Tally (after round 47): GREEN 11 · YELLOW 15 · RED 0 · GRAY 1** — Gate 15 YELLOW→GREEN: the five remaining named error-hiding sites closed (KnowledgeWorkspaceView dashboard+search, IntegrationHealthPanel, ExecutePanel targets+feed, WorkforceProvider installs, Settings crash-consent) with 12 new ui-tests and an executed 7-fail negative control; the UI error-hiding sweep is closed, backend-tier diagnostics helpers documented as non-blocking.
**Tally (after round 46): GREEN 10 · YELLOW 16 · RED 0 · GRAY 1** — Gate 16 YELLOW→GREEN: all 40+1 remaining coalesced-write stores joined the shutdown barrier via 16 subsystem registrations (coverage-locked, 15-fail-before-fix), and `powerMonitor` suspend now drains the same barrier (suppression-safe). LIVE-VERIFIED 2026-08-30: graceful quit logged `ran:23, failed:[], timedOut:[]` (was 7) on the real app, armed `out/` preserved via alternate outDir.
**Tally (after round 45): GREEN 9 · YELLOW 17 · RED 0 · GRAY 1** — Gate 5 YELLOW→GREEN: round 44 closed B-1 (`EnterpriseModulesList` filtered by per-module read permission, `dd85012`) and round 45 closed E-11 (silent mutations on the governance surfaces — decision approve/reject and Customize unit/role/governance toggles now say their refusals; 8 new ui-tests with an executed negative control). Every named Gate-5 driver is closed.
**Tally (after round 43): GREEN 8 · YELLOW 18 · RED 0 · GRAY 1** — Gates 4, 7 and 11 YELLOW→GREEN (all named HIGHs closed with tests + commits: split-brain switcher verified fixed; provenance write-failure + updated-count; vault-in-backup + backup-registry completeness + migration-ordering). Gates 6, 15, 23, 24 had their named HIGHs closed this round but stay YELLOW on lower-tier residuals (medium error-hiding sweep; org/workspace name uniqueness; the balance of the 14 negative scenarios). See §E. The remaining YELLOWs are: external/machine-blocked (20 Windows interactive sign-in, 8 cloud-live AI keys), product decisions (two onboarding systems, nav placement, name uniqueness), or catalogued lower-tier polish; and GRAY 22 (performance — never a defect, only unmeasured). Full main **901 files / 9403 passed / 7 skipped / 0 failed**; UI **44 files / 286 passed**; typecheck node+web + lint clean.

---

## B. FINAL RELEASE GATE (Gate 27) — disposition of every named release blocker

**Verdict (round 40, 2026-08-15): GO for macOS release-candidate `1.0.0-rc.20`; Windows remains a NO-SHIP platform until the machine-blocked acceptance run executes.**

The original blocker list, item by item:

1. **AI routing promise** — FIXED and live-proven. Tenant clamp at the single assembly site (round 34), fail-closed boot router (35), recovery-triggered reconfigure (39); a fresh local-only install answered through local Ollama with execution-stamped `Local` provenance on the driven run (Gate 26).
2. **Renderer honesty layer** — FIXED across the named clusters (rounds 36–39): admin error states + people CRUD (Gate 5), error-not-emptiness on the worst screens (Gates 12/15), assistant list + boot-race retry (26). Remaining medium-tier sweep items are catalogued in Gate 15 and are not release-blocking.
3. **Windows runtime** — BLOCKED, dispositioned. No Windows machine is reachable from this environment. The from-commit installer discipline, SHA-256 record, W-10 diagnostics, and step-by-step procedure (`WINDOWS-ACCEPTANCE-ROUND32.md`) are ready; **Windows ships only after that run passes.** This is a platform hold, not an unknown.
4. **Provisioned-org owner protection** — FIXED (round 40, this gate). `Organization.ownerUserId` recorded at provisioning; user patch/delete guards key on the target org's recorded owner; spec roles created built-in (Owner role no longer deletable); legacy stores heal on load when unambiguous, admit it when not; seeded-org behavior unchanged. Pinned by `provisionedOwnerProtection.test.ts` (8 — the Round 9 takeover replayed one tenant over, every step refused), which also closes Gate 24's top untested negative scenario.
5. **Vault corrupt-file + shutdown flush + restore** — FIXED where release-blocking (rounds 33/37): quarantine-not-reseed on the org/workspace/audit stores, flush barrier live-verified (`ran:7`), atomic restore that enforces its restart. Connector/secret vault reset-on-corrupt remains a Gate 11 YELLOW (keys are re-enterable; no tenant data loss).
6. **Onboarding resume/forfeit** — FIXED (round 36) and re-verified live on the real app during the Gate 26 driven run.

---

## C. ROUND 33 — FIXED THIS SESSION (all tested, full suite green)

| Fix | Files | Tests |
|---|---|---|
| Quarantine-not-reseed for org chart, workspaces, governance audit trail (CRITICAL + 2 HIGH) | `orgStore.ts`, `workspaceStore.ts`, `governanceStore.ts` | `round33StoreQuarantine.test.ts` (6) |
| Failed background write re-marks dirty (was: silent permanent loss) | same 3 + `enterpriseRecordStore.ts` | retry case in above |
| Auth: network-failure refresh no longer deletes the vault; restore never re-sends a consumed token; single-flight refresh (3 HIGH) | `auth/authService.ts` | `auth/authService.test.ts` (4 — first direct coverage ever) |
| O-11 fail-closed email predicate at the 3 remaining sites (`canSwitchTo`, `resolveActor`, `memberIn`) | `tenantContext.ts`, `authzGate.ts`, `tenantDirectory.ts` | existing round-31 suites + full regression |
| `second-instance` crash + no-reopen; tray restart `app.exit`→`app.quit` (flush bypass); `window-created` metric at the real call site | `main/index.ts` | — (bootstrap file remains untested; noted Gate 1) |
| Version collision: rc.17 tree vs published rc.17 tag → **1.0.0-rc.18** | both `package.json`, `CHANGELOG.md` | verifier |

## D. TOP REMAINING BACKLOG (P1, evidence in the nine domain reports)

1. AI routing: wire tenant preference into the route plan (`min(platform, tenant)` law exists and is tested; nothing calls it on the request path) + reconfigure on `setPreference` + reconcile display/router provider defaults. *Needs a design decision on per-tenant clamping at request time.*
2. Provisioned-org owner row + Owner role protection (structural markers needed — guards currently key on seeded constants).
3. `useActiveOrg()` hook distinguishing empty-vs-failed; adopt in the 5 org.list→[] sites; Administration error state; family-dashboard/module-screen/search error surfacing.
4. Vaults (`connectorVault`, `secureStore`) → quarantine-not-reset; add to backup registry; decrypt-failure must not unlink unrelated secrets.
5. `will-quit` flush barrier over all stores + `platform.dispose()`; restore-forces-restart on the install-wide path.
6. Onboarding: resume from persisted profile; Sign-In must not forfeit; final-step error surfacing; Escape/back.
7. dp:provenance redaction + module read gate (mirror `dp:preview`); provenance write failure must propagate.
8. Boot: renderer ready-gate or early registration for `xp:profile.get`-class channels; composition-failure surface; crash capture during boot.
9. Windows: run `WINDOWS-ACCEPTANCE-ROUND32.md` on the physical machine (artifact re-built as rc.18 required first — the rc.17-stamped gate artifact is now version-orphaned).
10. `--accent` token type fix (15 sites) + focus traps + Windows toolbar gutter.

---

## E. ROUND 43 — REMAINING RED/YELLOW BACKLOG PASS (2026-08-30)

Reproduced-then-fixed the concrete, actionable HIGHs across the YELLOW backlog, each as its own committed batch with tests + full-suite green. Prior gates (7 dp:provenance in `9082f56`'s predecessor `6ab3586`, 10 Security in `ec01b08`) were closed just before this pass.

| Batch | Gate(s) | Root cause → fix | Files | Tests | Commit |
|---|---|---|---|---|---|
| A | 7 Data | provenance write-failure swallowed by `persist().catch`; update-only imports counted as 0 | `importer.ts`, `dataCommandCenterModel.ts`, `panels.tsx` | `provenanceWriteFailure.test.ts` (3), model +2 | `9082f56` |
| B | 11 Database | ~27 customer/tenant/audit stores outside backup; migrations run after stores load | `storePaths.ts`, `migrationEngine.ts`, `releaseOps/index.ts` | storePaths +1, migrationEngine +5 | `dcee913` |
| C | 6/15 Business/Errors | module screen swallowed denied read into "No … yet" and denied save into silence | `EnterpriseModuleScreen.tsx` | `enterpriseModuleErrors.test.tsx` (4) | `bf1302e` |
| D | 23/24 Integrity/Sec-regression | non-atomic org provision with no rollback; two named negative scenarios untested | `provisionOrganization.ts`, `orgStore.ts`, `enterprise/index.ts` | multiOrg +3, orgStore +3, authzGate +2, secureBridgeTimeout +2 | `bac4eb2` |
| E | 4/9 + matrix | split-brain switcher verified fixed; logger-redaction residual verified resolved; matrix refreshed | `PRODUCT-READINESS-MATRIX.md` | — (verification) | this commit |

**Verification each batch:** affected unit tests, then full main (`901 files / 9403 passed / 7 skipped / 0 failed` at the end of Batch D), UI (`44 / 286`), typecheck node+web, ESLint `--max-warnings 0` — all clean. Negative controls where a bug was fixed (Gate 7 provenance redaction, persist propagation).

**EXTERNAL-VERIFICATION-BLOCKED (code-ready, cannot be forced GREEN here):**
- **Gate 20 (Windows):** boot/IPC/tenant/persistence matrix passed on real Windows 11 ARM64; the sign-in-gated interactive flows (B3/B5/B6/B9) need the cloud auth backend, unreachable from the offline VM. Acceptance procedure ready (`WINDOWS-ACCEPTANCE-ROUND32.md`); GREEN needs a networked Windows run that signs in.
- **Gate 8 (cloud-live AI):** local Ollama path is LIVE-verified and both cloud protocols are wire-proven; Anthropic/OpenAI live completions need real API keys — gated tests exist and skip. Run `NP_LIVE_AI=1 ANTHROPIC_API_KEY=… OPENAI_API_KEY=… npx vitest run src/main/ai/liveProviderVerification.test.ts`.

**DELIBERATELY DEFERRED (product decisions / lower-tier, not defects):** org/workspace name uniqueness (rejecting duplicates changes existing flows); the second onboarding system and Enterprise/Marketplace nav placement (product decisions, Gates 12/13); the medium-tier renderer error-hiding sweep (Gate 15, none release-blocking); the Slack Socket Mode dead code + `ws` dep (Gate 9/25 — removing a shipped dependency is out of scope for a readiness pass); Gate 22 Performance (GRAY — unmeasured, no verified defect).

---

*Per-domain evidence reports (file:line for every claim) live in the session transcripts; this matrix is the integrated summary. Fixed items above are in the round-33 diff; round-43 items are in commits `9082f56`, `dcee913`, `bf1302e`, `bac4eb2` and this commit.*
