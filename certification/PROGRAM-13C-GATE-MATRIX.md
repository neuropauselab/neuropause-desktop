# PROGRAM 13C — GATE MATRIX

One row per gate. `EVIDENCE` names what was executed. `NOT TESTED` means nothing
was executed and is never a synonym for "probably fine".

Baseline commit `927b7bf` · certification changes in `round18.patch` ·
12 August 2026 · execution environment: Linux container (desktop/backend suites),
`windows-latest` runner (packaged artifact), macOS arm64 (Round 17 runtime gates).

| # | Gate | STATUS | TEST COMMAND | ENVIRONMENT | EVIDENCE |
|---|---|---|---|---|---|
| 1 | Native launch, packaged artifact | **PARTIAL** | `gh workflow run windows-release.yml` | windows-latest | Run 31633030913 success; installer sha256 `693ae976…a587b`; payload `0x8664` PE32+; D-5/17g/17h strings present in `app.asar`. Never launched by a human. |
| 2 | Real A/B/C tenants | **PASS** | console harness `P13C.all()` | macOS, packaged-adjacent dev run | Round 17: seeded org 1/4/1, tenants A/B/C 0/0/0 on scoped projections. |
| 3 | Cross-tenant matrix | **PARTIAL** | `vitest run src/main/tenancy/crossTenant.test.ts` | Linux container | 52 tests incl. WRITE / DELETE / IDOR / SEARCH / COUNT. In-process against real stores; the running-application matrix was not run. |
| 4 | Runtime ownership | **NOT TESTED** | — | — | Requires the running application. |
| 5 | Retention | **NOT TESTED** | — | — | Requires the running application. |
| 6 | Background principal | **NOT TESTED** | — | — | Requires the running application. |
| 7 | Queue identity | **NOT TESTED** | — | — | Requires the running application. |
| 8 | Restart persistence | **PASS** | relaunch + `P13C` counts | macOS | Round 17: 115 users / 44 / 2 stable across restart. |
| 9 | Forced termination | **PASS** | `SIGKILL` + relaunch | macOS | Round 17: counts stable after forced kill. |
| 10 | Real tenant backup/restore | **FAIL** | census + `f22ProductionCoverage.test.ts` | Linux container | `createTenantArchive` / `restoreTenantArchive` / `registerTenantDomainSource` have **zero production call sites**. No tenant-scoped backup exists in the product. |
| — | D-5 AI policy intersection | **PASS** | `vitest run src/main/tenancy/round17TenantAiPreference.test.ts src/main/ai/tenantAiPreferenceCompose.test.ts` | Linux container | 9/9 exhaustive intersection + 7 composition tests + `NC-D5-ELEVATE` negative control. |
| — | F22 tenant-domain coverage | **PARTIAL** | `vitest run src/main/tenancy/f22ProductionCoverage.test.ts` | Linux container | Denominator 19 correct. Production registration **0/19 before this run, 6/19 after**; 13 uncovered and reported. |
| — | Channel → store | **PARTIAL** | `vitest run src/main/tenancy/channelStoreCoverageGate.test.ts` | Linux container | **2/194 = 1.0%**; 192 undeclared. Ratchet gate added. |
| — | D-6 authorization error contract | **PASS** (was NOT TESTED; 2026-08-31) | `vitest run src/main/ipc/denialCodeContract.test.ts` + `vitest run --config vitest.ui.config.ts ui-tests/denialContractD6.test.tsx` | Linux container / macOS | **CLOSED — see `D6-AUTHORIZATION-ERROR-CONTRACT-EVIDENCE.md`.** Root cause was not "unimplemented": main already threw TYPED denials (`AuthorizationError` carrying `permission`; `TenantContextError` carrying the 8-valued `TenantRefusalReason`), and `secureBridge.ts`'s catch flattened them to `new IpcError(err.message)` one frame before the boundary, after which Electron serializes only `message`. Measured blast radius was **8 renderer sites, not 3** — incl. `HoldsView.tsx:79-81`, which set `denied` on ANY rejection, so a crash told the user their role lacked access. A 432-file sweep for all 8 tenant reason codes and all 6 canonical denial sentences returned **zero** renderer hits: there was no discriminant at all. Fix: `classifyDenial` reads the TYPE (and reuses `TenantContextError.reason` rather than inventing a parallel vocabulary — mapping only the **five of eight** reasons that are genuinely authority answers; `not_loaded`/`no_workspace`/`workspace_orphaned` stay faults, because calling a cold start "you lack access" is a false claim); the code rides out as a stripped message prefix and is re-attached at the renderer's existing single `invoke` chokepoint. Prose matching survives as an explicit FALLBACK only where no code is present (the REST gateway calls `runSecureHandler` directly), so no denial banner regresses to blank. 25 pins (20 contract + 5 end-to-end through the REAL `BusinessView`); the central pair sends the SAME wording — matching none of the replaced regexes — stamped ⇒ denial state, unstamped ⇒ fault state. Negative controls both restored byte-identically. Full main 913/9542/7 (delta exactly +20), UI 59/359 (+5), tsc node+web clean, `electron-vite build` exit 0. `packages/shared/` (FROZEN) untouched. |
| — | D-7 silent write paths | **CLOSED for the six named paths** (was PARTIAL; 2026-08-31) | `vitest run --config vitest.ui.config.ts ui-tests/silentWriteD7*.test.tsx` | macOS | **See `D7-SILENT-WRITE-PATHS-EVIDENCE.md`.** The six named paths are resolved: FOUR were genuine user-facing silent writes and are fixed (`OperationsProvider.setFlags` — which had TWO failure modes, the rejection AND a resolved-`null` refusal the catch never sees; `SandboxProvider.generateReport` and `.setSchedule`; `WelcomeView.restartTour`, plus two siblings the register never listed). **TWO were not what the register said.** `SandboxProvider:280` (`cancelExecution`) has NO CALLER anywhere — a census over 3,707 files — so no click can reach it and it is not a user-facing silent write; it was given the same shape and is recorded, not counted. `WorkspaceContextProvider:90` is `flushSnapshot`, a 400ms-debounced layout save with no button behind it, and is structurally unfixable in place — the provider sits OUTSIDE `ToastProvider` (`App.tsx:43/45`) and renders nothing but its children, so error state there would render nowhere. The real defect of that class is `WorkspaceSwitcher`'s shared `run` helper, which had NO catch at all; one catch closes create/rename/delete/switch. Canonical Round-17h pattern followed (one channel per screen, cleared per write, set BESIDE the log, `role="alert"`); `SandboxView`'s banner gained the role it never had. 14 pins; UI 359→**373** (delta exactly the new files); full main **9579/7 unchanged**; tsc node+web clean; build exit 0. Four negative controls all fired, all restored byte-identically. **D-7b: SITE 1 (`EnterpriseModuleScreen`) CLOSED 2026-08-31** — the filed `.catch` was not even the dominant failure: `hold:resolve` refuses by RESOLVING null, the archive result was discarded so a refused archive closed the hold with a false note, and a refused delete fell through as success; fixed with 8 pins and 4 negative controls incl. one proving `onChanged()` unmounts the message. **D-7b: SITE 2 (`ConnectionProvider:117/120/129` — `resumeSync`/`pauseSync`/`syncNow`) CLOSED 2026-08-31** — all three swallowed IPC rejections with `.catch(() => undefined)`, so a `cloud:manage`-denied click did nothing and said nothing; and `pauseSync`'s "Sync paused" toast sat INSIDE `.then`, so a failed pause produced neither toast nor error. Each refusal now raises an ANNOUNCED `error` toast (role="alert", aria-live="assertive") carrying the D-6-cleaned boundary message VERBATIM, scoped to the awaited write so a success side-effect can never fire a false failure; per-action dedupe keys (`sync-pause`/`sync-resume`/`sync-now`) so a pause failure and a resume failure coexist; and the "Sync paused" toast is now gated on `status.online === false` so the no-active-org no-op (EMPTY_SYNC_STATUS, online:true) no longer claims a pause it did not achieve. 8 pins (`ui-tests/silentWriteD7bConnection.test.tsx`); 3 negative controls all fired (NC-1 swallow→5 fail; NC-2 gate removed→1 fail; NC-3 shared key→1 fail), file restored byte-identically (sha256 `9c684cbb…`); UI 381→**389** (delta exactly the new file), full main **9579/7 unchanged**, tsc node+web clean, build exit 0. Recorded, NOT fixed (out of this site's scope): `syncNow`'s engine resolves a transport failure as `state:'error'` rather than rejecting, so it is surfaced by the pre-existing degraded-connection warning, not this catch. **D-7b: SITE 3 (`EnterpriseView:135/142` — `personalization.favorite`/`saveView`) CLOSED 2026-08-31** — the star/pin header buttons swallowed IPC rejections with `.catch(() => undefined)`; both channels are `dashboard:read` + requireAuth, so a not-signed-in / non-member / under-permissioned click did nothing and said nothing. Each failure now raises an ANNOUNCED `error` toast (role="alert", aria-live="assertive") with the D-6-cleaned boundary message VERBATIM and a per-action dedupe key; the success arm (`setPersonalization`, which fills the star) is unchanged. 5 pins (`ui-tests/silentWriteD7bEnterpriseView.test.tsx`); 2 negative controls fired (NC-1 swallow→4 fail; NC-2 shared key→1 fail), file restored byte-identically (sha256 `8107cb45…`); UI 389→**394**, full main **9579/7 unchanged**, tsc node+web clean, build exit 0. Recorded, NOT fixed: the `recent` visit-tracking writes (`EnterpriseView:107/127`) are a SECONDARY effect of a succeeded navigation (`setTab`), left silent per precedent; a persist-time disk failure is swallowed MAIN-side (`personalizationStore.ts:108-109`, log-only) and cannot reach the renderer; and a newly-found SIBLING — `enterprise/PersonalizationPanel.tsx:58` (Remove-favorite via its `run` helper) — fails silently by a DIFFERENT mechanism (unhandled rejection, not `.catch(()=>undefined)`), a separate file left for its own site. **D-7b: SITE 4 (`BusinessFamilySection:162` — `personalization.favorite`) CLOSED 2026-08-31** — the per-module star button swallowed the rejection with `.catch(() => null)` + an `if (state)` guard, so a `dashboard:read`-denied click did nothing and said nothing. The refusal now raises an ANNOUNCED `error` toast (role="alert", aria-live="assertive") with the D-6-cleaned boundary message VERBATIM, keyed per module (`business-favorite:<id>`); the success arm (`setFavorites`, which flips the star) is unchanged, and the star is never updated optimistically so it correctly stays put on failure. 3 pins (`ui-tests/silentWriteD7bBusinessFamily.test.tsx`); NC-1 (restore the swallow) fired (2 fail), file restored byte-identically (sha256 `4338156b…`); an existing test (`businessGate6.test.tsx`) was wrapped in `ToastProvider` (the component now legitimately needs it, as it always has in the real app) — same 5 pins, no behavioral change. UI 394→**397**, full main **9579/7 unchanged**, tsc node+web clean, build exit 0. Recorded, NOT fixed: the reads at `:130/:142/:148` are load-on-mount, not writes; a persist-time disk failure is swallowed MAIN-side (`personalizationStore.ts:108-109`). **D-7b: SITE 5 (`WelcomeView` nested pilot `completeStep`) CLOSED 2026-08-31** — the pilot toggle's PRIMARY write (`ipc.pilot.setEnabled`) succeeds, then the nested `ipc.onboarding.completeStep('pilot').then(setStatus).catch(() => undefined)` swallowed its failure, so pilot mode turned on but the checklist step silently stayed incomplete (and reappeared unchecked on reload). The nested failure now surfaces through the screen's existing `actionError` `role="alert"` slot with a TRUTHFUL message ("Pilot mode is on, but the setup step could not be marked done…" — it must NOT claim the toggle failed, because it did not); `setPilot`/`.then(setStatus)` preserved, and `setActionError(null)` added at the top of the toggle for consistency with `complete`/`restartTour`. `OnboardingCompleteStep` is a public, unaudited, install-scoped channel (no auth/tenancy/consent/audit boundary touched). 2 pins — failure (announced + truthful) + success (`checklistPilotDone`, no alert) — `ui-tests/silentWriteD7bWelcomePilot.test.tsx`; NC (restore the swallow) fired (1 fail on the announced test), file restored byte-identically (sha256 `329e8b29…`). UI 397→**399**, full main **9579/7 unchanged**, tsc node+web clean, build exit 0. Recorded, NOT fixed (separate, lower-severity, no persisted inconsistency): the feedback export/submit silent-ish paths. **D-7b: SITE 6 (`PersonalizationPanel.tsx:58` — Remove-favorite) CLOSED 2026-08-31** — the shared `run` write helper had NO catch, so a refused personalization write (`dashboard:read` + requireAuth) rejected UNHANDLED and the Remove/Clear/Rename/Delete click cleared its spinner and said nothing. Fixed at the ROOT (the shared helper, per the WorkspaceSwitcher precedent — one catch closes all four writes): `run(key, label, op)` now try/catches the awaited op and raises an ANNOUNCED `error` toast (role="alert", aria-live="assertive") with the D-6-cleaned boundary message VERBATIM, keyed per action+item (`personalize:<key>`); `onMutate(next)` runs only after a successful op (outside the try), so a setState throw can't raise a false toast and on failure the list is left unchanged (no optimistic update). 3 pins (`ui-tests/silentWriteD7bPersonalizationPanel.test.tsx`); NC (restore the no-catch helper) fired (2 fail), file restored byte-identically (sha256 `6e7551e9…`). UI 399→**402**, full main **9579/7 unchanged**, tsc node+web clean, build exit 0. Actor-scoped write (`currentActorId()`), no auth/tenancy/consent/audit/fail-closed boundary touched. **D-7b COMPLETE — all six enumerated sites CLOSED** (Site 1 EnterpriseModuleScreen · Site 2 ConnectionProvider · Site 3 EnterpriseView · Site 4 BusinessFamilySection · Site 5 WelcomeView pilot · Site 6 PersonalizationPanel). Recorded-not-fixed, separate lower-severity items outside the D-7b enumeration: WelcomeView feedback export/submit. A `no-restricted-syntax` rule was assessed and deliberately NOT adopted: it would force mass suppression of legitimately-silent catches. |
| — | Backend scope | **NOT TESTED** | `vitest run --config vitest.integration.config.ts` | Linux container + Postgres 16 + Redis 7 | Confirmed IN SCOPE (`orgClient.ts:43`, `backendClient.ts:40`). 2 files / 17 tests pass; that is a smoke test, not a certification. 0 commits since 10 Aug. |
| — | Fresh running-app red team | **NOT TESTED** | — | — | Requires the running application. |

## Automated baseline (all executed this run)

| Suite | Result |
|---|---|
| Desktop node | 768 files / 8031 tests / 0 failures |
| Desktop UI | 11 files / 116 tests / 0 failures |
| Backend unit | 37 files / 418 tests / 0 failures |
| Backend integration | 2 files / 17 tests / 0 failures |
| Typecheck node / web | clean / clean |
| Lint `--max-warnings 0` | clean |
| Working-copy integrity | 2416 files, digest identical to the committed tree |
| Benchmark | **FAIL on this hardware** — 128.1ms vs 100ms budget; 18.6ms on the reference Mac. D-9. |

## Decision

Required gates 1, 3, 4, 5, 6, 7, 10 are not all PASS. Channel→store is not
complete. **D-6 was closed on 2026-08-31** (see the row above and
`D6-AUTHORIZATION-ERROR-CONTRACT-EVIDENCE.md`); this paragraph is preserved as
the state it described at the time. The red team was not run.

```
PROGRAM 13C  =  NOT CERTIFIED
```
