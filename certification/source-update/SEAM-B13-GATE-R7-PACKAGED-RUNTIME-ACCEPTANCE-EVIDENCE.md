# SEAM-B.13 / GATE-R.7 — PRODUCTION ARTIFACT GENERATION + PACKAGED RUNTIME ACCEPTANCE

## §1 Scope
Carry the governed `finance.journal.post` (module E4 · composition E3 · runtime E3) through the ACTUAL
electron-builder packaging path and accept the PACKAGED application under a fresh isolated runtime.
B.8/B.9/B.10 not reopened. **Verdict: `PRODUCTION_ARTIFACT_GENERATED_AND_PACKAGED_RUNTIME_ACCEPTED`**
— scoped exactly to finance.journal.post through the examined packaging and packaged-runtime path.

## §2 Operator decision
**Explicitly entered via the in-session question (not inferred): `CONSUME_AND_REARM`** for the armed
`out/`, and **rebuild at `1.0.0-rc.20`** (zero source mutation; same-version-different-bits recorded,
distinguished by provenance). The §45/§46 non-build branches did not apply.

## §3 Custody
HEAD_AT_START `a7a7d51` (the B.12 docs commit — expected) · 1 worktree · staged 0 · unstaged: only the
protected ` M certification/baseline.json` · GH_TOKEN UNSET · kernel tarball untouched · frozen files
untouched throughout (gate-detector PROCEED for the one new harness path before creation).

## §4 Source parity
**7/7 governed-file hashes = the recorded B.10/B.11/B.12 values, verified BEFORE the build and again at
gate close** — the artifact packaged exactly the proven source. The only test-side change this gate: the
read-back driver's hardcoded `JE-B10-01` store assertion generalized to the `JE-B` probe prefix after it
tripped on the B.13 profile (classified TEST_FIXTURE_MISMATCH — harness coupling, not product; the
mandatory read-back test had already passed).

## §5 Build command
ONE controlled build (§57 honored — no build-fix-rebuild loop; first attempt succeeded):
`env -u NP_E2E_BUILD sh -c 'node scripts/generate-notices.cjs && node scripts/generate-build-info.cjs
&& npx electron-vite build && npx electron-builder --mac --arm64 --publish never
-c.directories.output=dist-seam-b13 && node scripts/verify-release-artifacts.cjs --dist dist-seam-b13'`
from `apps/desktop`. Exit 0. The B.12-measured first-class output override isolated ALL packaging
outputs into `dist-seam-b13/` — **`dist/` byte-untouched (26 entries before and after)**.

## §6 Build environment
node v20.20.2 · npm 10.8.2 · electron 42.8.1 · electron-vite 2.3.0 · electron-builder 26.15.3 ·
macOS (Darwin 25.5.0) arm64 · built 2026-08-24T11:38–11:44Z. BUILD_NETWORK_ACTIVITY: none observed
beyond local toolchain operation (no dependency installation ran; `--publish never` proven in source at
B.12 — `isPublish` false unconditionally). APPLICATION_EXTERNAL_EFFECT: 0 (separately, below).

## §7 Side-write census
Pre/post measured. EXPECTED consumed: `out/` (armed 87-file ceremony build → 81-file release build —
the authorized CONSUME) · `resources/build-info.json` (stale `efe8196` record → current; old hash
`e67958cc…` recorded) · notices regenerated byte-identical (repo-root `THIRD-PARTY-NOTICES.md` sha
`6f7b8933…` unchanged — **no tracked-file dirtying; the dependency closure was unchanged**). Git status
after build: only the protected baseline.json + the two intended untracked additions (`dist-seam-b13/`,
the new harness). **UNEXPECTED_BUILD_SIDE_WRITE: none.**

## §8–§9 Artifact inventory + hashes
`dist-seam-b13/`: `NeuroPause-arm64.dmg` **135,529,165 B** (PRIMARY, sha512 `d044a9e2755e1efbfd537b6404…`)
· `NeuroPause-1.0.0-rc.20-arm64-mac.zip` **134,662,264 B** (UPDATER PAYLOAD, sha512 `2fcdd6c75aa285cae4…`)
· `beta-mac.yml` (UPDATER_METADATA, sha512 `8a9622aa3e470651…`) · two `.blockmap`s · `builder-debug.yml`
· `notarization-status.json` (skipped/credentials-absent, 11:44:03Z) · `mac-arm64/NeuroPause.app`
(unpacked; `app.asar` 58,969,724 B, sha256 `4add8d3fcc0104bac83c7b2a…`). Full sha512s in the gate log
manifest. **`verify-release-artifacts` PASS ×7 — the feed's sha512/size entries match the generated
binaries exactly (no ARTIFACT_METADATA_MISMATCH).** Version 1.0.0-rc.20 · arch arm64 · channel beta ·
provenance `commit: a7a7d51-dirty` (honest — the protected baseline.json makes the tree dirty; §7 of
the directive honored: never manufactured clean).

## §10 Package content parity — MEASURED ON THE ARTIFACT BYTES
python byte-scan of `app.asar` (aliased-grep instrument artifact caught and replaced, §2 #24):
`journalPostTransition` 1 · `journalEntryModule` 8 · `journal-post:` mint 1 ·
`finance-journal-post-policy-1` 1 · `postedByThisTransition` 2 · `journal-post-transitions.json` 1 ·
`enterprise:finance-journal-entries` 1 · kernel require 1 — **and the kernel ITSELF is inside the
package** (`APPROVAL_REQUIRED` 5 · `modelCannotSelfAuthorize` 10 · `duplicateSuppressed` 28 ·
`NP:UNOBSERVABLE` 2 · `RECONCILIATION_REQUIRED` 7). Note: the identifier `journalPostEvidence` is
bundled away by minification (0 hits) while its behavior-bearing literals (the provenance source string
and the evidence connector namespace) are present — classified precisely, not glossed.

## §11 Release strip — MEASURED ON THE ARTIFACT
All test sentinels **0** in the asar: `NEUROPAUSE_E2E_SEED_v1` · `e2eSeed` · `firstRealSendGuard` ·
`mockGraph` · `s16VerifyRun` · `e2eVerifyRun` · `NP_E2E_BUILD`. PACKAGED_TEST_SENTINELS = 0.

## §12 Publish firewall
`--publish never` in the command; `isPublish` false unconditionally (B.12 source proof); GH_TOKEN UNSET;
feed written locally only; nothing uploaded, released, or published.

## §13 Signing / notarization — MEASURED
`codesign -dv`: signed, `TeamIdentifier=J3G89MY3QG`, hardened runtime (`flags=0x10000(runtime)`),
embedded signature. `spctl --assess`: **rejected** (unnotarized). `notarization-status.json`: skipped,
credentials absent. **Classification: SIGNED · NOT_NOTARIZED · Gatekeeper NOT accepted — a LOCALLY
EXECUTABLE PRODUCTION ARTIFACT, not distribution-ready** (§40 honored).

## §14–§16 Fresh profile · boot · composition
Harness `e2e/journalPackaged.e2e.cjs` (new, PROCEED-class) launched the PACKAGED binary
(`dist-seam-b13/mac-arm64/NeuroPause.app/Contents/MacOS/NeuroPause`) on fresh `mkdtemp` profiles.
**`app.isPackaged === true` asserted from the running app** — a packaged launch, not dev.
ISOLATED_USERDATA realpath-verified. All five boot-composition logs from the packaged process:
`Enterprise OS ready` · `Owner bound to the active principal` · `Runtime core ready` ·
`Background services started` · `Read-back reconciler started`.

## §17–§19 Authority · policy · journal post (TEST A)
The real packaged renderer door (`window.neuropause.invoke` → `enterprise:module.*` → secure bridge →
RBAC → governed `runAction('post')` → kernel). TEST A: exact success message, **rev 1→2 exactly**,
`postedAt 2026-08-24T11:45:36.747Z`, evidence row ALLOW/executed/VERIFIED_SUCCESS with terminal and
**`effectTime === postedAt` byte-for-byte**, per-attempt transition id
`journal-post:rec_922463e7…:1:1787571936747-1` — **the B.9 fix present and live in the packaged
artifact**.

## §20 Refusal (TEST B)
NOT_EXECUTED — structurally unreachable through any production door (the B.10-measured positive
finding: `createAuthorize` throws before `runAction` when no session exists; local mode always carries
a principal). The DENY path stays module/composition-proven. No production code was modified to
manufacture a negative (§29 of the directive honored).

## §21 Replay (TEST C)
Door replay refused (`'JE-B13-01 is already posted.'`), rev unchanged, **physical write count 1**.

## §22 Concurrency (TEST E)
Two genuinely concurrent posts (single renderer turn, `Promise.all`): one success, one refusal at the
fresh authoritative re-read; **rev exactly +1**; results recorded verbatim. Kernel-loser variants
(STALE/HOLD) remain module/composition-proven (unchanged bound). TEST D (refusal-then-retry) and TEST F
(lying executor) NOT_EXECUTED at the packaged level — they require injection; the B.9 fix they protect
is evidenced by the per-attempt ids above and the composition pins.

## §23–§24 Read-back · reconciler
**Separate-process read-back (mandatory): PASS** — the env-gated vitest driver over the packaged
profile reconstructed `VERIFIED_SUCCESS` from persisted bytes via a fresh reader (`matches: 1` per
transition), verified per-attempt id shape, provenance oracle, no non-success row carrying a success
terminal, and **`awaitingVerification === false` on every row** (the packaged app's own reconciler
service had ticked over the same store, journal rows structurally invisible).

## §25–§26 Restart (TEST I) · replay after restart (TEST J)
Packaged app terminated and relaunched on the same profile: posted state, **`postedAt` byte-identical**,
rev 2, evidence and idempotency all durable; replay after restart refused with no second write.

## §27 Failure map
Carried unchanged from B.11's measured map (policy DENY `AUTHORIZATION_FAILURE` · approval HOLDs ·
STALE_RESOURCE · idempotency's three surfaces · executor-throw HOLD `OUTCOME_UNKNOWN` → IN_FLIGHT →
`RECONCILIATION_REQUIRED` · UNKNOWN stays UNKNOWN). **TEST K executed on the ISOLATED profile: a
corrupted `journal-post-transitions.json` prevents packaged boot entirely** (no window; fail-closed,
whole-app blast radius — the B.11 finding CONFIRMED at package level) **and restoring the ledger
recovers boot** (the corruption was the sole cause). KNOWN LIMIT retained; NOT fixed (§33 honored).

## §28 External effect
**EXTERNAL_EFFECT = 0.** No email/M365/finance/webhook/notification/third-party call; all writes inside
the temp profile and `dist-seam-b13/`.

## §29 Artifact maturity
finance.journal.post: module **E4** · composition **E3** · runtime **E3** · **production artifact E3** ·
**packaged runtime E3** · **production acceptance E3**. Not promoted: distribution-readiness,
notarization, multi-process concurrency, any other action/module/connector, enterprise production,
regulatory/security certification (§59 honored).

## §30 Known limits
App-fatal corrupt-ledger boot (now package-confirmed) · importer posted-row content mutation
(ACCEPTANCE_ADJACENT_IMMUTABILITY_DEBT — §34: the behavior ships in the artifact, PRESENT, unchanged) ·
success-branch comment drift · CONCURRENCY_SCOPE = single-process event loop + unconditional
single-instance lock (plugin/processAdapter trust bounds stated) · SIGNED but NOT_NOTARIZED, Gatekeeper
rejects (not distribution-ready) · same-version rebuild (rc.20) distinguished only by provenance
(`a7a7d51-dirty` vs `efe8196`) · TESTS B/D/F NOT_EXECUTED at package level (structural/injection
reasons, module-proven) · public claims stay quarantined (§35: Hitavada ceiling; patent =
application-in-process; legal-name conflict unresolved).

## §31 Final verdict
**`PRODUCTION_ARTIFACT_GENERATED_AND_PACKAGED_RUNTIME_ACCEPTED`** — the §51 progression was walked in
order: generated → hashed → content parity → booted → real door → governance → execution → persisted
read-back → restart → replay → concurrency. **RE-ARM COMPLETE (the CONSUME_AND_REARM consequence):**
after all acceptance evidence was captured, `NP_E2E_BUILD=1 npx electron-vite build` ran as the LAST
build; §43 verified — `out/` = 86 files, seed chunk `e2eSeed-Bq2bRg9M.js` present, sentinel ×1, at HEAD
`a7a7d51`, re-armed 2026-08-24 17:18:25, `out/main/index.js` sha256 `e40a47a2051b6e2e8aa90450…`.
**NP-008 ARMED = TRUE** (content-hashed chunk names differ from the consumed 20-Aug build — the
PROPERTY, not the hash, is the invariant, per the NP-014 lesson). NO FURTHER BUILDS.
Regression at final source state: **892 files / 9328 passed / 7 skipped** (identical totals; the
driver's generalized assertion lives behind the env gate). Custody at close: 7/7 governed hashes OK;
frozen surfaces untouched; baseline.json preserved uncommitted.

## §32 Next gate
The operator's call: (a) notarization/distribution envelope for this artifact class (credentials
required — a standing human gate), or (b) the next unproven domain action through the same E4→E3 ladder,
or (c) the B1/B2 executor-governance FG envelope (frozen `cst/`, token required). Artifact disposition:
`dist-seam-b13/` RETAINED on disk, untracked, hashes recorded here; temp profiles removed after evidence
capture.
