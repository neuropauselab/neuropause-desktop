{
  "name": "@neuropause/solution-packs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "NeuroPause Solution Pack SDK (IP-01) — pure types + validation + registry for industry solution packs layered on the certified enterprise core. No Node/Electron/DOM. Wired into the desktop + release gate in IP-02.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.23.4"
  },
  "devDependencies": {
    "vitest": "^1.6.1"
  }
}
