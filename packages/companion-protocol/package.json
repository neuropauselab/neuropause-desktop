{
  "name": "@neuropause/companion-protocol",
  "version": "0.1.0",
  "private": true,
  "description": "NeuroPause Companion protocol (Mobile M1-02) — end-to-end sealed envelopes, QR pairing, replay protection, and wire framing shared by the desktop Companion Gateway and the mobile app. Pure TypeScript: no Node, no Electron, no DOM (Hermes-portable).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@noble/ciphers": "^2.4.0",
    "@noble/curves": "^2.4.0",
    "@noble/hashes": "^2.4.0",
    "@scure/base": "^2.4.0",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "vitest": "^1.6.1"
  }
}
