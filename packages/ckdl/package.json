{
  "name": "@neuropause/ckdl",
  "version": "0.0.0-preview.1",
  "private": true,
  "description": "NeuroPause Constitutional Knowledge & Decision Layer (NCEA 11.1) — the governed semantic foundation on the Enterprise Runtime: one enterprise knowledge graph, decision graph, evidence engine, explainable trust model, objectives, and decision intelligence. Every decision has evidence; every recommendation has provenance; nothing fabricates certainty. PREVIEW; in-memory, no vector/embedding backend.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@neuropause/runtime": "*",
    "@neuropause/cloud-core": "*",
    "@neuropause/shared-cloud": "*",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^1.6.1"
  }
}
