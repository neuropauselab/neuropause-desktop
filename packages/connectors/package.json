{
  "name": "@neuropause/connectors",
  "version": "0.0.0-preview.1",
  "private": true,
  "description": "NeuroPause Enterprise Connector & Automation Platform (NCEA 10.4) — one governed connector runtime: SDK, auth framework, secret vault, registry, triggers, automation, marketplace foundation. Every connector is auditable + observable. PREVIEW; no live integrations.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@neuropause/runtime": "*",
    "@neuropause/cloud-core": "*",
    "@neuropause/shared-cloud": "*",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^1.6.1"
  }
}
