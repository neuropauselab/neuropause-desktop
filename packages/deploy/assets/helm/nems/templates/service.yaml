apiVersion: v1
kind: Service
metadata:
  name: {{ include "nems.fullname" . }}
  labels:
    {{- include "nems.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "nems.selectorLabels" . | nindent 4 }}
