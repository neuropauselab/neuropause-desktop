{
  "name": "@neuropause/cli",
  "version": "0.3.0",
  "description": "NeuroPause CLI — manage credentials, drive the Enterprise API, inspect the graph/timeline/automation, and publish to the marketplace.",
  "license": "UNLICENSED",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "bin": {
    "neuropause": "./dist/cli.js"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@neuropause/sdk": "*",
    "@neuropause/shared": "*"
  }
}
