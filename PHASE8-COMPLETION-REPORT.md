# Phase 8 — Release Candidate Hardening: Completion Report

**NeuroPause Desktop · v1.0.0-rc.14 lineage · 2026-08-07 · continues from `a2f9df5`**
Authority: the five Stage A reports (recon, completion, gap analysis, pilot readiness, backlog). Every item below is either executed, verified-already-present, or explicitly deferred with its reason recorded in `ROADMAP.md`. Verification: cloud compile smoke (typecheck shared/desktop-node/desktop-web/backend, eslint `--max-warnings 0`, full electron-vite build — all green) + the full test suite and manual pass on the development Mac at landing.

## Wave 1 — Pilot data safety (8.1–8.4) ✅

The data-loss class is closed. **8.1** The spurious root `recharts@3` pin (collision under the Phase 7 dashboards) is removed. **8.2** A store-path registry (`main/storage/storePaths.ts`) is now the one source of truth for what backup protects: prefix patterns cover **every enterprise-module store — all 104 today and any registered tomorrow — plus the executive, governance, automation, assistant, feedback, onboarding, flags, pilot, license and update stores**; the backup manager consumes the registry (17 hand-listed paths before → the full store surface now), and the pre-migration snapshot inherits coverage automatically. Locked by a coverage test naming every critical store. **8.3** The parse-or-reset era is over: a store envelope distinguishes first-run (ENOENT) from corruption (file **quarantined beside itself, bytes preserved**, never silently reset) from future-versioned data (set aside intact on downgrade); the enterprise record store and five services read through it; writers stamp `schemaVersion`; migration 0002 stamps existing stores (data version 2), protected by the engine's pre-run backup — which now includes business data. **8.4** Packaged builds finally produce runtime logs: a rotating `logs/app.log` sink (attached at boot, before bootstrap), one bounded-append helper behind app/crash/audit logs (size rotation, generations kept, never-throws), and support bundles reveal in the file manager. New tests: storePaths (4), storeEnvelope (7 incl. real record-store quarantine + migration 0002), boundedLog (3), backup business-domain restore (1).

## Wave 2 — Release discipline (8.5–8.8) ✅

**8.5** The `internal` update channel — selectable but feedless, a permanent silent update failure for anyone who chose it — is no longer offered (metadata gains a `selectable` flag; the Settings card renders only published channels) and a persisted `internal` preference **heals to `beta`**; a new test locks every resolvable channel onto a feed the pipeline actually publishes. **8.6** `scripts/bump-version.cjs` (+ `npm run version:bump`) moves both manifests atomically, refuses regressions, and prints the tag flow — removing the manual two-file step that mislabeled rc.2–rc.13; CHANGELOG is backfilled (Unreleased/Phase 8 · Phase 7 · rc.14 Final Wave · consolidated rc.2–rc.13) and the false `[1.0.0] GA` heading is corrected with an in-place note; the current build's changelog section is **baked into build-info** and surfaced as "What's new in this build" in Release Diagnostics (the generic feed carries no notes — the build now does). **8.7** `verify:release` runs inside every local package script (an unverified local artifact is no longer possible) and `package:mac:universal` exists for Intel coverage. **8.8** `scripts/generate-notices.cjs` walks the production dependency closure and writes `THIRD-PARTY-NOTICES.md` (repo + bundled via extraResources) as the first step of every package script — the OSS-attribution gap is closed structurally, not once.

## Wave 3 — Electron platform modernization (8.9) ✅ (runtime validation on target hardware at landing)

Electron **30.5.1 → 42.8.1** (inside the supported 41–43 window; 43.3.0 is latest), electron-builder **24 → ^26.15.3**, @electron/notarize **→ ^3.1.1** (the hook already used the v3 API shape — no change). The main-process compat sweep found **zero** usage of any API removed or changed between 30 and 42 (no protocol registration, no BrowserView, no setPreloads, no traffic-light or Rosetta APIs; session/webContents usage is domain objects, not Electron). Cloud compile smoke on the new toolchain: typecheck (node + web + shared + backend) 0 · eslint 0 · full electron-vite build 0. Deliberately **not** ridden along: electron-vite/vite/vitest/ESLint-9 generation (one toolchain risk at a time — ROADMAP). Mac landing exercises the runtime + `package:dir` packaging validation.

## Wave 4 — Pilot truth (8.10–8.14) ✅

**8.10** INSTALLATION.md no longer lies: it states the real signing state per build, gives genuine Gatekeeper steps for unsigned pilot builds, and names real artifacts; QUICK-START now describes the actual product (six sidebar groups, Business as the primary surface, ⌘K, the 13 families). **8.11** README rewritten for repository truth (rc.14, the 3-app/44-package map **with the dormant-tier warning as the lead orientation fact**, the module framework section, real gates, honest caveats; broken links gone); `certification-matrix.csv` regenerated **from the certification test itself** (104 rows, source-of-truth column); all 21 stale 2026-07-24 program reports + the DD report now carry historical-snapshot banners naming the current locks. **8.12** End-user documentation exists for the first time: `docs/user/` — Finance, HR & Payroll, Inventory & Procurement getting-started guides plus a one-page Pilot Orientation, each walking the real flows (chart of accounts → invoice → payment → journals; employee → attendance → payroll → filings; product → PR → PO → receipt with the budget/contract gates). **8.13** `docs/legal/` carries EULA + Privacy Notice drafts (clearly marked for counsel), and first run now presents them: a `legal` onboarding step (timestamped acknowledgment through the existing onboarding store) linking to the bundled documents. **8.14** The app can finally open its own documentation: a fail-closed help surface (fixed `HELP_DOC_IDS` enum → catalog-resolved bundled paths only, PUBLIC-classified, zero arbitrary-path/URL surface) + a Documentation card in Getting Started; the docs set ships in the package via extraResources.

## Wave 5 — Operational visibility + hygiene (8.15–8.20) ✅

**8.15** The dead telemetry stub is **deleted** (zero producers, unreachable consent — worse than absent), the Settings group is relabeled **"Crash reports"** to say what it actually toggles, and the no-usage-telemetry posture is now documented product truth (README, privacy notice, pilot orientation). **8.16** Startup is measured: boot-phase marks (app-ready → window-created → runtime-core-ready) through a pure, tested recorder, logged at boot completion and rendered in Release Diagnostics — budgets stay advisory on CI by design (the knowledgeBench wall-clock lesson). **8.17** The stale `COMMIT_QUEUE.md` runbook is harvested into **`ROADMAP.md`** (C2d–C6 Finance depth, deferred ledger, procurement list) and deleted along with the root MANIFEST residue and the prototype HTML. **8.18** The dormant 39-package tier is **quarantined from the release-critical path**: `typecheck:release` / `lint:release` / `test:release` cover exactly the wired workspaces and both release workflows now run those — a signed release no longer gates on ~910 tests of code no binary ships (dev-wide scripts unchanged; disposition decision recorded in ROADMAP). **8.19** `scripts/bootstrap-env.cjs` (+ `npm run bootstrap:env`) creates `.env` from the example (never overwrites, generates real random secrets, 0600); the demo-data seeder is deferred with its reason (needs interactive verification against live module guards — ROADMAP). **8.20** Offline licensing was **verified already present** (the validator persists and reloads a last-known-good snapshot with shared grace evaluation — the Stage A flag was wrong on this point); its load path now uses the quarantine envelope for consistency.

## Updated Pilot Readiness

The four Stage A blockers are resolved on the repo side: pilot business data is inside backup/restore/migration-rollback with version-stamped, quarantine-protected stores; field issues are diagnosable (rotating app log in every redacted support bundle, revealed in Finder, startup timings in diagnostics); the binary platform is current (Electron 42) with verification wired into every packaging path; and a business user has a truthful install guide, real per-family documentation reachable in-app, first-run legal acknowledgment, and an honest feedback loop. **Remaining pilot gate items are human-gated only:** Apple signing/notarization credentials (pipeline ready, fail-closed), the live update-feed host, counsel sign-off on the legal drafts, and connector OAuth registrations for pilot scope.

## GA Readiness

RC → GA needs, beyond the pilot gate: signed+notarized artifacts proven through the full pipeline on real credentials; the first tagged release with the new discipline (`version:bump` → CHANGELOG → tag → CI verify); pilot-driven fixes; the deferred UX items that carry daily-use weight (tables, forms, sidebar 2.0); Finance depth C3 (functional currency) for non-USD pilots; live connector traffic exercised against real tenants; and the toolchain generation (ESLint 9 / vite / vitest) landing green. None are architectural.

## Scores (evidence-based, not aspirational)

**Production readiness: 8.5/10** — data durability, release discipline, and platform currency are now real; unexercised signing and an untagged lineage hold back the rest. **Security: 9/10** — fail-closed IPC classification at boot, keychain vault refusing plaintext, audited privileged actions on a bounded log, redaction at rest, fail-closed package installs and notarization; deducted for audit-log chain (primitive exists, not yet on the IPC log) and no SIEM/WORM export (both in ROADMAP). **UX: 8/10** — grouped nav, live real-data dashboards, honest empty states, in-app docs, first-run flow; tables/forms/sidebar-2.0 deferred items are the gap. **Performance: 7.5/10** — instrumented startup, tested metrics core, 10+ bench suites, virtualized lists; no long-session soak, budgets not yet calibrated on target hardware.

## Repository statistics (at delivery)

104 certified modules / 13 families (test-locked) · 47 workspaces, release gate scoped to the 5 wired ones · desktop ~223k LOC with **630+ test files** after Phase 8 (+15 new tests across 5 new suites) · zero TODO/FIXME debt markers · zero `@ts-ignore` · CHANGELOG current · README truthful · 22 historical reports bannered · docs: +6 pilot-facing documents, all bundled and in-app reachable.

## Final remaining work

Everything open lives in **`ROADMAP.md`** with reasons: Finance depth C2d–C6; the deferred UX/tooling items; the dormant-tier disposition; the human-gated procurement list. If a Phase 9 is opened, it starts from that file — which is built ONLY from repository evidence.
