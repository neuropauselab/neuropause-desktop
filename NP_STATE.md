# NP_STATE.md — operational state (read after CLAUDE.md, before work)
Living, TRACKED working doc (committed each slice; excluded from the freeze source spec — see DECISIONS.md D-5). Mirror of CLAUDE.md §1 with operational detail.

## Now
- HEAD at the NP-005 commit · FREEZE INTACT (`BASELINE-e19eb88e096c`) · branch `cert/data-import-cst-integration`.
- **NP-005 · GOVERNED DEV LOOP A0–A3 LITE BUILT (consolidated directive, 19 Aug 2026; ceremony HOLD untouched).** Package + brain-lane correction ACCEPTED by the operator; §0.3 condition honored (dated CORRECTION note appended to the Phase-0 evidence with commits `59f9087`/`2315d33`). **A0** `AUTONOMY.md` (CLAUDE.md the ONE constitution — references, never duplicates, CLAUDE wins on conflict; task states with BLOCKED legitimate + UNKNOWN automatic; GREEN/YELLOW/RED; scope budget ~15/0/0/0/0; MAX_REPAIR_ATTEMPTS=2→BLOCKED_BY_REPEATED_FAILURE; plan-before-code; the anti-workaround rule VERBATIM; A4–A9 RECORDED NOT BUILT, post-ceremony). **A1** `WORK_QUEUE.md` seeded to reality (NP-000 ceremony AUTHORITY_REQUIRED/HELD/outranks-all-at-step-1 · NP-CORR-001 + NP-001–004 COMMITTED w/ evidence · NP-005 · NP-006 blocked on NP-000; growth by operator directive/approved proposal ONLY). **A2** `certification/gate-detector.sh` + NEW authoritative `certification/frozen-surfaces.json` (machine projection of the §2/§6 gate registry; fail-closed on missing metadata; self-tests 4/4: frozen→STOP · sensitive/effect→GATE · clean→PROCEED · mixed→frozen dominates). **A3** `certification/honesty-scanner.sh` (report-only; 8 pattern classes; self-tests: clean 0 findings / hostile all 8 caught). YELLOW reported: freeze-script excludes +AUTONOMY.md +WORK_QUEUE.md — INTACT preserved. Full main **858/8992/3** (zero product source touched). Evidence: `…NP-005-DEV-LOOP-A0-A3-EVIDENCE.md`. **⛔ Ceremony HOLD stands — next input: the operator sitting, step 1.**
- **WHILE-HELD WORK PACKAGE EXECUTED (merged directive, 19 Aug 2026) — S23 kit + calendar dry run + S39 + release overlay; the CEREMONY HOLD UNTOUCHED.** §1 **S23 first executable slice:** `capabilities/certificationKit.ts` (PURE typed record + `runKitChecks`, the seven ruled artifacts, retroactive from mail.send; kit-complete ≠ certified) + back-fill (5): mail.send completes with a REAL oracle entry; four refusal fixtures refuse at the REAL propose core (incl. `UNSUPPORTED_ACTION` for calendar.create — the boundary live at the edge today). §2 **calendar.create DRY RUN (10) — PROPOSALS ONLY, nothing past ADMIT_FOR_ASK:** the honest-plan path live for the first time ("UNVERIFIABLE today: needs a calendar read-back oracle (event GET-by-id corroboration)" rendered verbatim); S5.1 with the PRODUCTION predicate REFUSES ('not a certified consequential capability'); kit-modeled FUTURE predicate (test-only) → ADMIT_FOR_ASK/'ASK' and nothing more; five S4.2 attack classes hold for the second capability; `deriveOracle` gains the honest per-capability needs registry (non-frozen, mail.send unchanged, liveBrain 95 green). NO frozen touch — the anticipated contract-entry FG gate NOT triggered (module-level dry run; a production calendar propose surface would be the gate). §3 **S39 F-S17-1 CLOSED:** `renderer/localFirst/story.ts` single vocabulary; door ("Try Free Locally" — a door, not a claim) ⇄ state (`LocalModeBanner` line — derived truth, local branch only) BY IDENTITY; claim-placement + honest-copy pinned (`localFirstStory.test.tsx`, 5); visible strings unchanged. §4 **release overlay RECORDED NOT ENTERED** in ROADMAP-HORIZON (v1.0 = twelve-component core + ONE governed capability; the ONE sequencing question explicitly open, §5 amendment after the ceremony). No fleets (ceremony can interleave; D-15). Full main **858/8992/3** · ui **40/276** · typecheck+lint clean. Evidence: `…WHILE-HELD-S23-KIT-CALENDAR-DRYRUN-S39-EVIDENCE.md`. **⛔ The S5.4 ceremony HOLD stands exactly as presented — next input: the operator sitting, walking the checklist from step 1.**
- **⚠️ HONEST CORRECTION (pre-ceremony recon) → FIXED + IN-APP PROVEN: the BRAIN-PROPOSE LANE was missing.** Phase-0's report overclaimed "brainReview live in the confirm panel" — renderer-side only; NO production caller invoked the L6 stack from propose (the FG-10 gate always SKIPped). Divergence rule honored: STOP → report → fix under the standing Phase-0 mandate (non-frozen; FG-9 field already landed). **Landed:** `liveBrain/brainProposeLane.ts` (+7 pins: gate-ADMIT-and-consume end-to-end over the REAL `l6ExecutionGate` · operator-edit→SKIP · expiry→observable DENIED · scope-null/cross-tenant→no proposal · display-honest fields · propose-side purity) — composes the REAL substrate (workspace snapshot [modules honestly UNAVAILABLE — registry accessor private] · capability graph over `mutationAssuranceFor` · S34a records) → S4-certified `buildProposal` from the VALIDATED artifact → stash → FG-9 fields; `runProposeM365ActionWithArtifact` (artifact never crosses IPC); async propose handler feeds the lane BEST-EFFORT/ADDITIVE-ONLY (any refusal → response exactly as today); `executionGate` exports `deriveAuthority`/`deriveOracle` (single source both sides) + logs `L6-GATE ADMIT/REFUSE` (Brain-proposed distinguishable from merely-governed in the running app). **`brainPropose.e2e.cjs` real-Electron PASS (0 failed):** propose→brainReview (eight fields, recipient named)→UNEDITED execute→ACKNOWLEDGED+`L6-GATE ADMIT`→read-back TERMINAL=VERIFIED_SUCCESS→second identical send NOT re-admitted (single-use in-app). `mailReadBack.e2e.cjs` re-run PASS. Full main **856/8977/3** · ui **39/271** · strip **PASS** (lane PRESENT in release as production code; seams absent). Ceremony steps 2/3/5/6 now real in-app in mock. **The bound ceremony go stands; the HOLD re-arms at step 9 — awaiting the operator at the keyboard.**
- **⛔ PHASE 0 + FG-10 ACCEPTED (operator, 19 Aug 2026) — FINAL CEREMONY CHECKLIST PRESENTED — ABSOLUTE HOLD.** The runbook's Ceremony section is now the FINAL CHECKLIST: 9 steps in execution order, each marked OPERATOR-ACTION vs MACHINE-ACTION (1 pre-flight/registration+consent+allowlist+latch=OPERATOR · 2 Brain proposes=MACHINE · 3 ASK renders/reads · 4 confirm=OPERATOR-ONLY · 5 governed at-most-once execution=MACHINE, FG-10→FG-4→governedSend · 6 independent read-back=MACHINE · 7 evidence attaches · 8 containment=OPERATOR · 9 THE GO=OPERATOR, the final line). The completion evidence carries the operator-required verbatim claim: "the L6 circle is mock/test-verified in the running app; no real external effect has occurred; the first real Brain-proposed action remains operator-gated." **Canon landed on the same directive:** CLAUDE §2 **#15** "Memory informs governance; memory never becomes governance" (a prior verified success is EVIDENCE, never permission; similarity never auto-converts to certainty). **Experience-memory arc RECORDED, NOT ENTERED** as LB-6's detailed design in ROADMAP-HORIZON (ExperienceRecord → reproduction fingerprint → six-valued outcome memory VERIFIED_SUCCESS/VERIFIED_FAILURE/UNKNOWN/DENIED/CANCELLED/EXPIRED → reproduction engine, similarity≠certainty → failure intelligence → reproduction-before-repair → validated knowledge after repeated independent evidence → self-awareness); NOTE: the directive said "LB-7" but operational memory is LB-6 in the recorded numbering — folded at LB-6, numbering kept, flagged. **Standing: no real contact before the ceremony; the checklist HOLD is absolute; report-and-continue ends here. Claude never supplies credentials/consent/confirm/send.**
- **S5.4 PHASE 0 — the READ-BACK CIRCLE closes in the RUNNING APP (mock, TEST-VERIFIED, non-frozen, ZERO real contact).** The missing half of the loop is built: after ACKNOWLEDGED, an INDEPENDENT read-back reaches a real terminal — executor-success is never the claim (§2 #14). Landed: (1) **`verification/verifyGovernedSend.ts`** — the DE-GATED, READ-ONLY orchestrator (latch/env/electron-free; builds the target from a plain send ref + drives pure `verifyEffect` over an INJECTED reader); 6 tests, three terminals + idempotency + read-only surface. (2) **`e2e/mockGraph.ts`** — PURE mock-Graph seam: records the send from the real `/me/sendMail` body, answers the read-back Sent Items/Inbox per `NEUROPAUSE_E2E_VERIFY` (success→VERIFIED_SUCCESS · bounce→VERIFY_FAILED · hold→HOLD, never auto-promoted); **circle proven over the REAL `verifyEffect`** (6). (3) **`e2eSeed`** delegates to `mockGraph` + fires the read-back on a captured send. (4) **`e2e/e2eVerifyRun.ts`** — compile-gated trigger over the REAL reader (sentinel `NEUROPAUSE_E2E_VERIFY_v1`). (5) **`s16VerifyRun`** latch coupling SEVERED to the gated caller (delegates to `verifyGovernedSend`). (6) **`verify-e2e-strip.sh`** extended (new chunks + sentinels) — **PASS** (seam absent from release; the READ-ONLY orchestrator ships un-stripped, no production caller yet — explicit gate: S22 reconciler is the caller). (7) **`e2e/mailReadBack.e2e.cjs`** — **real-Electron run PASS (0 failed, 3 launches):** each knob drove certified path → ACKNOWLEDGED (mock Graph, not real) → independent read-back → the expected terminal in the main log (`success`→VERIFIED_SUCCESS + corroborated `<mock-…@MOCK.OUTLOOK.COM>` id · `bounce`→VERIFY_FAILED · `hold`→HOLD, asserted not auto-promoted). Full main **855/8970/3**; typecheck node + lint clean; INTACT (`BASELINE-52008b68ddb5`). Evidence: `…L6-S5-4-P0-READBACK-CIRCLE-EVIDENCE.md`. **Phase 0 panel wiring earlier this pass:** `response.brainReview` renders the eight fields in the confirm panel (`M365WritePanel`/`EntraConnectorPanel`; `m365WritePanelBrainReview.test.tsx`, ui 39/271). **REMAINING before HOLD: the FINAL CEREMONY CHECKLIST (S5.4), then HOLD for the operator's keyboard + go.** Claude never supplies creds/consent/confirm/send.
- **FG-10 CLOSED — the L6 execution gate is in the governed execute path.** Token honored. Choreography: non-frozen accompaniment (`liveBrain/executionGate.ts` `l6ExecutionGate`) → **INTACT #1 `0ced65c0283a`** → the one gated call in the frozen `connectors/index.ts` mail.send branch (`const l6 = l6ExecutionGate(deps, r); if (!l6.ok) return l6.refusal;`, before the FG-4 guard) → **INTACT #2 `8755bf876636`**. 5 conditions honored: no-proposal→SKIP (existing main **853/8958** + ui **268** unchanged) · refusal is observable DENIED (like FG-4) · single-use · gate≠observer (order: FG-10→FG-4→governedSend) · re-derivation via shared deriveAuthority/deriveOracle+admitForExecution (proposal's words never re-trusted). `executionGate.test.ts` (5) + `phase0Wiring.test.ts` (7). Evidence quotes the token. **REMAINING Phase 0 (non-frozen, report-and-continue):** panel wiring (`response.brainReview`→`BrainReviewCard`) · e2e mock executor + mock read-back seams (extend `e2eSeed`, strip discipline) · production read-back de-gate (READ-ONLY pin; verify-e2e-strip keeps meaning) · real-Electron e2e (extend `mailSend.e2e.cjs`) proving the circle in-app with 3 terminals. Then FINAL CEREMONY CHECKLIST + HOLD (operator keyboard/go). ZERO real contact throughout.
- **S5.4 PHASE 0 building (mock, report-and-continue) — non-frozen core LANDED; FG-10 PENDING for the execute-gate.** Seam map done (read-only recon): Phase 0 is mostly NON-FROZEN — no new `packages/shared` touch (FG-9 `brainReview` + frozen `writeStates` already present); the one frozen touch is the execution-time gate inside `connectors/index.ts` (a frozen surface, FG-4/FG-5 gated it) → **FG-10**. **Landed (non-frozen):** `liveBrain/toBrainReview.ts` (Proposal→FG-9 brainReview display fields, honest verification incl. UNVERIFIABLE) + `liveBrain/proposalStore.ts` (`stashProposal`/`gateL6Execution` keyed by a RE-DERIVABLE fingerprint tenant+capability+account+params, so no `M365ActionExecuteRequest` frozen touch; `admitForExecution` re-derivation; single-use; no proposal→SKIP=assistant flow unchanged). `phase0Wiring.test.ts` (7); full main **852/8953/3**; INTACT (`BASELINE-92421db3a697`). **⛔ FG-10 PRESENTED** — a production gated call to `gateL6Execution` in the `connectors/index.ts` mail.send branch before `governedSend` (refuse on re-derivation mismatch; skip=unchanged), FG-4-style; awaiting operator token. **Remaining Phase 0 (non-frozen, after FG-10):** panel wiring (`EntraConnectorPanel`/`M365WritePanel`→`BrainReviewCard` renders `response.brainReview`); e2e mock executor + mock read-back seams (extend `e2eSeed` mock Graph to the read-back GETs) + strip discipline (`verify-e2e-strip.sh`); production read-back de-gate (move s16 read-only orchestration out of `e2e/` to `verification/`, sever the latch coupling, edit strip, pin read-only); the real-Electron e2e (extend `mailSend.e2e.cjs`) proving the circle in-app with 3 terminals. Then final ceremony checklist + HOLD.
- **S5.3 FULL MOCK LOOP LANDED — ⛔ HELD at the S5.4 hard stop (runbook presented).** `s5MockLoop.test.ts` wires the whole chain through the REAL modules (`composeLiveBrainState → buildProposal → admitForExecution(ASK) → actionRecord.observe → verifyEffect → recordVerification → m365WriteStates`); ONLY the executor result + Graph reader are mocked. Three outcomes exercised (first-class failure): **VERIFIED_SUCCESS → EXTERNALLY_OBSERVED moves; VERIFY_FAILED + HOLD(UNKNOWN) → recorded, does NOT move** (uncertainty is never success). **Scope (operator ruling): FULL CHAIN PROVEN AT MODULE LEVEL over the real modules; ZERO real contact — never "e2e".** Five-state movement asserted from the STORE alone; **store→panel transitively proven via the FG-7 truthful-surface pins** (no app run needed). The production L6 wiring (app call-site→propose-handler→panel) is **NOT BUILT** → moved to **S5.4 PHASE 0** (the running-app mock proof, behind FG gates). `s5MockLoop.test.ts` (4); full main **851/8946/3**; INTACT (`BASELINE-09486d4268bf`). **⛔ S5.4 RUNBOOK presented (`…L6-S5-4-FIRST-REAL-BRAIN-ACTION-RUNBOOK.md`) — HELD; begins with PHASE 0.** **PHASE 0 (front, before any ceremony step):** wire L6's certified proposal into the REAL propose→confirm→CST→admission→governedSend path + prove the whole circle IN THE RUNNING APP in mock (real-Electron, extending S14) + production read-back wiring; any frozen touch → FG gate FIRST; MOCK-only (report-and-continue). **NOT BUILT.** Then the ceremony prereqs (Phase 0 green · fresh Azure registration+consent at operator keyboard · FG-4 allowlist+latch renewed, recipient=operator's own address, single-send/no-retry · explicit operator go). Claude never supplies creds/consent/confirm/send. No real contact until Phase 0 is green in the running app + all prereqs met + explicit go. Awaiting operator direction to build PHASE 0 next.
- **FG-9 CLOSED — S5.2 ASK SURFACE LANDED (mock).** Token honored (`AUTHORIZED: FG-9 — CapabilityProposeM365ActionResponse brainReview review fields, per gate doc`). Choreography: non-frozen checkpoint (`BrainReviewCard` + truthful-surface ui-tests) → **INTACT #1 `BASELINE-bdd7aa037112`** → EXACTLY the additive optional `brainReview?` field on `CapabilityProposeM365ActionResponse` ok-branch (`packages/shared/src/ipc/contracts.ts`, no existing field touched) → **INTACT #2 `BASELINE-0d4c1418ecdf`**. Pins: additive-only+fallback (absent→panel as today) · DISPLAY-ONLY (8 review fields verbatim, no callable/confirmed/authority, renderer never re-derives) · EXPIRY-AT-CONFIRM (main-side in `admitForExecution`, not greyed UI). `brainReviewCard.test.tsx` (3) + `proposalExecutionBoundary.test.ts` (10); full main **850/8942/3** · ui **38/268** · typecheck node+web + lint clean. Evidence quotes the token. **NEXT (report-and-continue): S5.3 — the FULL mock loop on the real-Electron e2e** (Brain state→proposal→ASK→confirm→CST→mock executor→mock read-back→verification attaches→five-state panel moves; VERIFIED_FAILURE + UNKNOWN→HOLD each exercised; ZERO real contact) → then **⛔ HOLD + present the S5.4 RUNBOOK** (first real Brain-proposed action = its own ceremony: fresh registration + consent + allowlist/latch renewed, explicit go).
- **S5 OPENED (staged). §0 assurance-verbatim + §1 LB-numbering + S5.1 EXECUTION BOUNDARY landed — ⛔ S5.2 HITS AN FG GATE.** §0: the assurance statement is verbatim in the S4.3 cert evidence. §1: ROADMAP-HORIZON stages relabeled **LB-6…LB-23** (no §5 collision). **S5.1** `liveBrain/proposalExecutionBoundary.ts` `admitForExecution(proposal, deps)` → ADMIT_FOR_ASK (DATA projection onto the existing `capability:m365.propose` input) or REFUSED; **ASK-only is STRUCTURAL** (no ALLOW branch in code — S13 pattern); **execution-time RE-DERIVATION** (authority/oracle/tenant/state-hash/expiry re-derived from the real substrate + compared to the proposal's claims; any mismatch→REFUSED; the proposal's words never re-trusted); single-path (types only, no executor/CST import). MOCK-ONLY, zero real contact. `proposalExecutionBoundary.test.ts` (10); full main **850/8942/3**; INTACT (`BASELINE-22f8493fe5b4`). **⛔ S5.2 (ASK surface) needs to deliver a certified Proposal to the confirm panel → a FROZEN touch on `CapabilityProposeM365ActionResponse` (`packages/shared/src/ipc/contracts.ts:500`) → FG-9 gate PRESENTED (additive optional `brainReview` review-fields, no existing field touched); awaiting the operator token. No frozen change applied.** Standing S5 hard stops remain (any real send = S5.4 ceremony · autonomy ≥L4 separate).
- **S4 CERTIFIED (S4.1 engine + S4.2 fleet-hardened + S4.3 ten proofs) — ⛔ NOW AT THE S5 HARD STOP.** S4.2 independent 9-attack fleet (D-15 quiet window, 12/12) **falsified 9 real defects** the by-construction claims missed (7 confused-deputy/trust): scope-vs-state escalation · cross-tenant evidence · request-controlled freshness window · proposalId collision · reject-reason leak · bridge global-route membership · authorityFor/oracleFor embedded unvalidated · empty-evidence vacuous. ALL fixed failing-test-first (HIGH set demonstrated RED→GREEN); confirmed claims recorded as positive verification; trust contracts (authorityFor/oracleFor/state-hash injected → S5 wiring must derive them) recorded. `L6-S4-2-INTEGRITY-FLEET-SYNTHESIS.md`. **S4.3 certified** the ten proofs (schema·tenant·authority·prompt-injection·evidence·risk·verification-plan·zero-authority·replay·determinism) each to a landed test — **"S4 CAN PROPOSE; IT CANNOT REACH EXECUTION"** (`…L6-S4-3-CERTIFICATION-EVIDENCE.md`). liveBrain proposal.test.ts (19) + purposeBridge (5) + s4TenantUnification (4); full main **849/8932/3**; INTACT (`BASELINE-65fe9e629b40`). **S5 REVIEW PACKAGE presented (`L6-S5-REVIEW-PACKAGE.md`):** governance ALLOW/ASK/DENY (ASK-only for consequential today; ALLOW unreachable without S28/S38) · certified-executor-only projection onto `capability:m365.propose`→governedSend (no second path) · derivation-source wiring (authorityFor←RBAC/CST, oracleFor←oracle registry, state-hash←real state) · production read-back (§2#14). **⛔ HARD STOP: no execution wiring until operator reviews the package; internal S5 hard stops remain (FG gate for any frozen/IPC surface · any real send = human+allowlist+latch like S15 · autonomy ≥L4 separate).**
- **S4.1 PROPOSAL ENGINE + L5 BRIDGE LANDED — awaiting the S4.2 quiet window.** `liveBrain/proposal.ts` `buildProposal(request, deps)` → a `Proposal` artifact (EXACTLY the reviewed 18 fields) or REFUSED/EXPIRED/BLOCKED. Data-only, deterministic (D-14), zero-runtime-import (types only). **Derivation rules make the 9 attacks pass by construction:** (a) authorityRequired DERIVED (no request field to inject); (b) verificationPlan DERIVED (no oracle→UNVERIFIABLE, never false VERIFIED); (c) evidence must resolve→else BLOCKED; (d) stale/state-changed→EXPIRED→HOLD; (e) tenant/scope→REFUSED; conflict→BLOCKED; hostile narrative/params inert. `proposal.test.ts` (17) pins all nine. `liveBrain/purposeBridge.ts` `bridgePurpose` (built WITH S4): maps ONLY the purpose's resolved capability if it is a governed route — nothing inferred merely because possible; `purposeBridge.test.ts` (4). §4 horizon addendum landed ("Learning remains separate from governance change — the Brain must never rewrite its own constitution"). Full main **849/8925/3**; INTACT (`BASELINE-7c94f43aeedc`). **NEXT: S4.2 · the 9-attack integrity fleet** — runs UNINTERRUPTED under a **D-15 quiet window** (requested; operator declares), each finding fixed failing-test-first → **S4.3 certification** (ten proofs; S4 CAN PROPOSE, CANNOT REACH EXECUTION) → HOLD. **S5 remains its own hard stop.** No execution wiring exists.
- **S4 OPENED (staged). §5 CANONIZED + §6 RECORDED + S4.0 LANDED (blocking precondition closed).** §5: **CLAUDE.md §2 #14** — universal read-back ("executor-success is never the claim"; every capability REQUEST→EXECUTION→EXTERNAL EFFECT→INDEPENDENT READ-BACK→VERIFIED_SUCCESS/VERIFIED_FAILURE/UNKNOWN, D-16 vocabulary, deny-by-default; ack≠verified; no oracle→UNVERIFIABLE; send-corroboration≠delivery). §6: **ROADMAP-HORIZON.md** records phases 9–26 as DIRECTION-NOT-ENTERED (capability ladder A–E; operational-memory/change/drift/incident/dependency/product/workforce/founder intelligence; predictive-only-after-verified-history; self-assurance; self-testing OS; NL layer; SDK; multi-node; edge; workforce non-surveillance constraint verbatim) — each enters §5 only via five-field amendment (freeze-excluded, D-5). **S4.0 · Tenant Authority Unification** (audit #1 blocker): `tenancy/tenantStamp.ts` (TenantStamp + reconcileTenant, deny-by-default); every snapshot (L1–L5) carries `tenant?: TenantStamp` (type-only additive); `composeLiveBrainState` conflict-check-0 → `tenant identity` / `tenant identity (evidence)` CONFLICTING + `tenantProvable`; `proposalAdmission.ts` `proposalAdmissible` = the S4 REFUSAL predicate. **BINDING fixture (`s4TenantUnification.test.ts`, 4): evidence A + target B → STATE=CONFLICTING, PROPOSAL=REFUSED, ACTION=NONE**; single-A→admissible; cross-tenant evidence→refused; unstamped→deny-by-default refused. Zero-import pin permits the two PURE authorities (verificationTerminals + tenantStamp). Full main **847/8908/3**; INTACT (`BASELINE-df58eee7e690`). **NEXT (report-and-continue): S4.1** proposal engine (reviewed field set, honest verificationPlan) + L5 bridge built-with-S4, consuming `proposalAdmissible`. Then **S4.2** 7-attack integrity fleet (D-15 quiet window) → **S4.3** certification. **S5 remains its own hard stop.** No proposal code exists yet.
- **CORRECTION-PASS HARDENING (operator rulings 1–4) DONE — S4 PACKAGE FINAL, AT THE HARD STOP.** Ruling 1 (reproduce-first F-5): each fake-green has an explicit reproduction test; **demonstrated RED→GREEN** (reverted the D-16 authority + discovery section → 6 reproduction tests failed; restored → all green, no residue). Ruling 3 (D-16): `verification/verificationTerminals.ts` is the **single classification authority** (deny-by-default); S1/S2/S19 import it, none redefine; **anti-re-entry invariant** fails the build if any consumer hardcodes a terminal; L6 zero-import pins refined to permit only this one pure value import. Ruling 2 (cross-check): **CLEAN BILL** — `m365WriteStates` counted only the correct success terminal (now via `isSuccessTerminal`); renderer `m365Outcome`/`operatorConsole` consume a SEPARATE send-outcome vocabulary, already honest. Ruling 4: S4 item 3 shows tenant-binding (confused-deputy) + VERIFICATION PLAN explicit `unverifiable-today` state; item 5 addresses injection. DECISIONS **D-16**. Full main **846/8904/3**; INTACT (`BASELINE-1eb69af5a02c`). **⛔ HARD STOP: complete S4 review package (`L6-S4-REVIEW-PACKAGE.md` + `…FLEET-AUDIT-SYNTHESIS.md`) awaits operator review before ANY proposal code; first S4 work = the B1 tenant precondition; S5 execution stop separate behind it.**
- **🔬 L6-S4 INDEPENDENT FLEET AUDIT DONE (D-15 quiet window, uninterrupted, 12/12, 0 err) — CORRECTIONS FOLDED, AT THE S4 HARD STOP.** Falsified **2 HIGH fake-greens** the self-review missed: (F1) S1/S2 matched `VERIFIED_FAILURE` but the real send oracle emits `VERIFY_FAILED`, and `HOLD`/`UNKNOWN`/`VERIFY_PENDING` were laundered → a verified-failure/unresolved verification read "okay". Fixed with canonical `classifyVerification`/`verificationCertainty` (deny-by-default) + tests re-pointed at the REAL vocabulary. +9 more (F2 discovery per-result UNKNOWN · F3 health folds incidents+pendingWork · F4 `could` scope-dispute/exact-id gate · F5 changeReport subject guard · F6 §4 fiction→design-contract · F7 scopeResolved-false-when-disputed · F8 zero-import pins hardened for bare+dynamic · F9 routedCaps scope-guard · F10 `authority`→`governedPaths` · F11 exact-id `could`). liveBrain **34** green; full main **845/8902/3**; INTACT (`BASELINE-5d1e725e6591`). `L6-S4-FLEET-AUDIT-SYNTHESIS.md` records confirmed=positive-verification + falsified=corrected. **S4 REVIEW PACKAGE COMPLETE** (`L6-S4-REVIEW-PACKAGE.md`): 1✅ audit · 2✅ S2/S3 · 3✅ proposal-schema worked example · 4✅ L5 bridge NOT BUILT + plan + rec **bridge-WITH-S4** · 5✅ zero-authority import sets + 5 landing-gate pins + **B1 tenant-safety precondition = the #1 blocker before any S4 code**. **⛔ HARD STOP: awaiting operator review of the package before ANY proposal code. First S4 work = the B1 tenant wiring precondition. S5 execution stop separate behind it.**
- **🧠 L6 LIVE BRAIN — S1 (state), S2 (context/provenance), S3 (reasoning) ALL LANDED — ⛔ NOW AT THE L6-S4 HARD STOP.** S2 `liveBrain/brainContext.ts` `assembleBrainContext` → BrainContext of atomic PROVENANCED FACTS (per-field {layer, evidence object}); provenance-completeness + traceability pinned; coherent with S1 (atomic KNOWN vs synthesized CONFLICTING); `brainContext.test.ts` (7). S3 `liveBrain/brainReasoning.ts` `reason(state, context, previous?)` → BrainReasoning (what-is-happening/why/what-changed/what-is-missing/known/unknown/could/should-not), every statement cites its evidence; **REASONS, never PROPOSES** — NO-PROPOSAL invariant pinned (no proposedAction/requiredAuthority/execute/grant field; `could` only points at S4); what-changed UNKNOWN without a prior (never assumed stable); `brainReasoning.test.ts` (8). All deterministic (D-14), zero-runtime-import. Full main **845 files / 8895 passed / 3 skipped**; typecheck+lint clean; INTACT each slice. **S4 REVIEW PACKAGE (`L6-S4-REVIEW-PACKAGE-CHECKLIST.md`):** item 2 (S2+S3 summaries) ✅ READY; item 1 (independent 12-lens fleet audit) AWAITING QUIET WINDOW — mechanism = declared QUIET WINDOW per D-15; items 3 (proposal-schema worked example), 4 (L5 operational-bridge status+plan — NOT BUILT, it is proposal-adjacent so behind the gate), 5 (zero-authority extended to S4) assembled at the gate. **HARD STOP: no proposal code until the operator reviews the package. Separate HARD STOP before S5 (executions).**
- **🧠 L6 LIVE BRAIN OPENED (staged, operator-approved 19 Aug) — SLICE 1 STATE MODEL LANDED.** `liveBrain/liveBrainState.ts` `composeLiveBrainState(inputs)` → ONE truthful operational state as a PURE JOIN over the real L1–L5 read-models + S34a ActionRecord; five-valued uncertainty first-class per section (KNOWN/UNKNOWN/UNAVAILABLE/CONFLICTING/VERIFIED); UNKNOWN never "probably okay"; CONFLICTING detected+surfaced (scope · routed-but-verified-failed · env-NEED-vs-routed), both claims kept, never auto-reconciled; risk UNAVAILABLE (no model, not fabricated). All 5 pins green (pure join/no store · fail-closed scope · provenance · zero-runtime-import · conflicting-not-reconciled); `liveBrainState.test.ts` (11) over REAL read-models. **CLAUDE.md §2 #13 canonized** ("The Brain proposes; it never reaches.") + **DECISIONS D-14 deterministic-first** (no LLM feeds any L6 slice until Phase E). **Substrate grounded by DIRECT READ** — the 12-agent fleet audit was aborted by intervening messages (background agents share the session abort signal); 6 claims CONFIRMED, 1 CORRECTED (`actionRecord.query` async/disk → `ActionRecord[]` injected), folded into evidence (`…L6-S1-SUBSTRATE-GROUNDING-AND-REVIEW.md` + `…L6-LIVE-BRAIN-SLICE-1-STATE-MODEL-EVIDENCE.md`). Non-frozen (no frozen surface touched); full main **843 files / 8879 passed / 3 skipped**; typecheck+lint clean. **Staged control points:** S2/S3 pre-approved to continue on this clean landing; **HARD STOP before S4 (proposals)** and a **separate HARD STOP before S5 (executions)**. **S15 containment COMPLETE (19 Aug):** app deleted → consent revoked · registration deleted · profile rm -rf'd (latch store gone → no retry).
- **L2/L3 LIVE WIRING LANDED** (non-frozen): `environmentModel/liveSources.ts` `liveEnvironmentSources` builds L2's `probe` from the REAL L4 `CapabilityGraphSnapshot` (route→HAVE · not-governed gap→NEED · missing/unknown→UNKNOWN · !scopeResolved→UNAVAILABLE) + REAL L1 `workspaceDomain` rollup (present+records→HAVE · empty→NEED · unavailable/absent→UNAVAILABLE) — proven against real `composeCapabilityGraph`(real `mutationAssuranceFor`) + real `composeWorkspaceDomain`/`toWorkspaceDomainField`; local-mode honesty pinned. `environmentDiscovery/liveSources.ts` `liveDiscoverySources` binds L3 authority to cheap CONSENT facts (workspace resolved + connected account) decided WITHOUT reading the catalog, and collection to the REAL `CapabilityDiscoveryService.catalog()` probe run only under granted authority — **never-a-silent-scan proven with a catalog call-spy** (never invoked without authority) + bound to the real service's fail-closed empty catalog. Honest boundary recorded: per-capability discovery-consent surface is a future increment (not invented). Both adapters type-only imports (read-only-adapter invariant pinned); cores' zero-runtime-import unchanged. `environmentModel/liveSources.test.ts` (5) + `environmentDiscovery/liveSources.test.ts` (5); full main **842 files / 8868 passed / 3 skipped**; typecheck + lint clean. Evidence: `…ENVIRONMENT-MODEL-DISCOVERY-LIVE-WIRING-EVIDENCE.md`. **Next:** production call-site (compose real L4 graph + L1 rollup + catalog under active scope, feed the live sources; close L3→L2→L5 loop); renderer surface would touch frozen `packages/shared`→FG gate. **L6 entry condition remains fully met** (L1–L5 each ≥1 closed slice) — re-presentation is a human review gate.
- **OS-track (latest):** L1 Slice-2 CLOSED (FG-8 `workspaceDomain` rollup). **L4 Capability Graph — Slice-1 + LIVE WIRING LANDED**: `capabilityGraph/liveSources.ts` binds `composeCapabilityGraph` to the REAL `mutationAssuranceFor` predicate + BRAIN-1 draft lane + the one certified workflow (`propose→governedSend`); five fields re-proven (`liveSources.test.ts`, 5); proposal-only invariant extended. Honest finding: `mutationAssuranceFor` is PER-CONNECTOR → microsoft-entra cohorts read governed-certified → MISSING (no lane/workflow), NOT NOT_GOVERNED (Slice-1 assumption corrected); NOT_GOVERNED proven with a non-certified connector; per-capability distinction is S23. **L5 Purpose Engine — Slice-1 LANDED**: `purposeEngine/purposeEngine.ts` `evaluatePurpose` → pure nine-state `PurposeEvaluation` (UNKNOWN→STATED→RECOGNIZED→ROUTED→PERMITTED→CONSENT_READY + off-ramps NEEDS_CLARIFICATION/UNSUPPORTED/DENIED), halts at first missing rung, UNKNOWN stays UNKNOWN, proposals-only, zero-runtime-import invariant pinned (`purposeEngine.test.ts`, 13). **L2 Environment Model — Slice-1 LANDED**: `environmentModel/environmentModel.ts` `composeEnvironmentModel` → pure PURPOSE-BOUND four-state model (HAVE/NEED/UNKNOWN/UNAVAILABLE); UNKNOWN never silently becomes HAVE; purpose-bound proven by a probe call-spy; zero-runtime-import (`environmentModel.test.ts`, 9). **L3 Environment Discovery — Slice-1 LANDED**: `environmentDiscovery/environmentDiscovery.ts` `runDiscovery` → purpose-bound pipeline PURPOSE→REQUEST→MINIMUM-REQUIRED→AUTHORITY→COLLECTION→CLASSIFICATION→EVIDENCE; **NEVER A SILENT SCAN** (collect() gated on granted authority, call-spy proves denied/unknown never collected); `discoveryRequestFor(model)` derives minimum-required from L2's NEED+UNKNOWN gaps (proven against a real L2 model); DISCOVER≠RECOMMEND≠BUILD; zero-runtime-import type-only coupling to L2 (`environmentDiscovery.test.ts`, 10). All four slices non-frozen, no FG gate. Full main **840 files / 8858 passed / 3 skipped**; typecheck + lint clean. Evidence: `…CAPABILITY-GRAPH-LIVE-WIRING`, `…PURPOSE-ENGINE-SLICE-1`, `…ENVIRONMENT-MODEL-SLICE-1`, `…ENVIRONMENT-DISCOVERY-SLICE-1`. **🎯 L6 ENTRY CONDITION FULLY MET — L1, L2, L3, L4, L5 each now have ≥1 CLOSED slice.** Per §5, L6 (Live Brain) may now be RE-PRESENTED with its five fields refreshed against the real substrate (approved-in-principle; entry was deferred until this condition — a human review gate, not autonomous).
- Suites (RUN against BASELINE-43dfbe3ff6f7): full main **8724/3 skipped** (823) · real-Electron e2e **13/13** (guard inert in mock mode) · guard+mode pins **16** · typecheck + lint clean · `verify-e2e-strip.sh` PASS (seed + guard absent from release).
- Landed: Slices 1–14 + **S15 EXECUTED**. S14 = real-Electron governed-loop e2e (mock Graph). S15 = first REAL send ATTEMPT at the operator's keyboard: **AUTHORIZED ✓ · SUBMITTED ✓ (Graph 202/ACKNOWLEDGED) · EXTERNALLY OBSERVED = PENDING** (automated read-back blocked by macOS Keychain, F-3). One email to the operator's own address; latch spent. FG-4 also fired live on a non-allowlisted attempt (negative proof). Profile-isolation guard landed (`996958d`, `--user-data-dir` required). Evidence: `…SLICE-15-FIRST-REAL-SEND-EVIDENCE.md`.
- **Slice 16 — DONE: FIRST VERIFIED_SUCCESS in product history.** Real in-session run (2026-08-18T13:25:54Z): TERMINAL=VERIFIED_SUCCESS, internetMessageId `<PN2P287MB1597…@…PROD.OUTLOOK.COM>`, bounce=none, attempts=1, corroborated Sent Items match. `verifyEffect` (17 pins incl. fault injection) + `m365ReadBack` + `s16VerifyRun`. No FG-5 (D-10); verify-e2e-strip PASS. Artifacts in `certification/s15-artifacts/`.
- **Slice 17 — FG-6 LANDED + renderer wall-kill (in progress; e2e unconfirmed).** Honest `local` AuthStatus + device-local principal via **FG-6** (token honored; INTACT #1 `BASELINE-3f19fb707fa0` → INTACT #2 `BASELINE-43238f789694`; then re-record at renderer `BASELINE-1dbef0d5bbdf`). `resolveGovernedActor` (`local:<id>` self-disclosing/forgery-denied/no-strip, S33 edge pinned) + `sessionEmailFor` (`local-<id>@device.invalid`, invalid-by-rule) + `localPrincipalStore` (INSTALL_GLOBAL, backup-registered). App.tsx `local` branch → full shell + `LocalModeBanner`. Suites: main **8766/3** (828) · UI **257** (35) · typecheck+lint clean · verify-e2e-strip PASS. DECISIONS D-11/D-12; CLAUDE §4 AuthStatus-exhaustiveness standing rule. **`local-mode.spec` GREEN 5/5** + **graceful cloud-absence LANDED** (`useIsLocalMode`/`CloudUnavailableLocal`/`LocalModeConnectProvider`; Org/Store/Devices/Billing show honest absence, not the "Sign in to manage…" red banner; semantic/livesync/license already graceful). UI 259 · typecheck+lint clean. **S17 CLOSED** (DoD walked in evidence). **F-S17-1** → S39.
- **BRAIN-1 CLOSED (①②③④, non-frozen, no FG gate; TEST-VERIFIED).** + CLAUDE §3 standing rule: any renderer-touching commit runs the FULL main suite (never the ui glob alone). §5 amendment draft revised to v2 (five-field acceptance standard per layer + Environment-Intelligence rules + canonical preamble) — awaiting approval. ① hostile-adapter CI gate (draft model gains no authority). ② `ai/brain/mailDraftGateway.ts` (model → zod → 1 repair → referenceDrafter fallback). ③ assistantService via `servingDraftMailer()` (reference-only). ④ `draftEval.ts` harness + eval report (honest zero-model baseline, 0 candidates) + DECISIONS D-13 + badge (names a model only when one serves) + LiteLLM reserved. Fixed a latent S17 `cssTokens` failure (LocalModeBanner `--surface`). Full main **8784/3** · UI **259** · typecheck+lint clean. Draft lane serves `referenceDrafter` until eval clears + operator go.
- **S34a — Queryable Action Record CLOSED (FG-5; TEST-VERIFIED).** `connectors/actionRecord.ts` + ONE gated observer line in frozen `connectors/index.ts` (INTACT #1 `322115041eeb` → INTACT #2 `8f0f1e137d3f`). Best-effort observer, never blocks the send; chain + fingerprints + `recordVerification`; tenant-isolated `query`. 13 tests (all 8 closing proofs + query proof + observer invariant). Full main **8796/3** · UI **259** · verify-e2e-strip PASS. Scope-fenced.
- **S19 — Counter truth CORE CLOSED (F-5 in logic; TEST-VERIFIED, non-frozen).** Definition pass first; five states (`connectors/m365WriteStates.ts`) derived from the single S34a `ActionRecord` store (no parallel counting). F-5 reproduced (old counter 0/never) + made truthful (7 tests). **Display landed via FG-7** (INTACT #1 58024ee849b0 → INTACT #2 959735ab2a6a): additive `ConnectorSyncSnapshot.writeStates`, unified/sync join, panel retires the old counter. **S19 CLOSED — F-5 end-to-end.** Full main 8809/3 · UI 262.
- **§5 amendment APPROVED per-layer** (L1–L5 into §5; L6 approved-in-principle, entry deferred). **FG-7 presented** (ConnectorSyncSnapshot five-state display — holds for the operator's token). **Workspace Foundation (L1) Slice 1 observable object LANDED** (`enterprise/workspaceFoundation/domainAggregate.ts`, READ/aggregate-only tenant-scoped domain snapshot; 5 tests; non-frozen). Honesty line fixed (S15/S16 LIVE). **Next:** WF live-wiring increment (registry + existing channel, non-frozen); FG-7 on token. **S15 containment COMPLETE (2026-08-19, operator keyboard):** enterprise application deleted → consent revoked · app registration deleted · S15 profile `rm -rf`'d (evidence copied out first; latch store gone → no retry possible). Recorded in `…SLICE-15-FIRST-REAL-SEND-EVIDENCE.md` §Containment.

## Change-control trail (S14)
```
9f4d7dd  freeze re-record — foundation INTACT
8c72836  alive(s14): e2e seed + mock-Graph seam (structurally absent) + verify-e2e-strip.sh
caac9c0  alive(s14): dispatch-vs-compose intent + assistant asks on ambiguity
0be757b  alive(s14): real-Electron governed-loop e2e (mock Graph) — S14-A/B/C green
f254f2d  fix(s14): seed uses authService.setStatus (renderer leaves the sign-in wall)
ef9c9f0  freeze re-record — INTACT (BASELINE-9f4d36abed4e)
```

## Next 3 steps
1. **S15 — first REAL email ⛔ human at keyboard.** Runbook prepared. HARD STOP before real credentials / OAuth / real send. The compiled-in recipient-allowlist guard lands here (gated if it must sit in a frozen send path).
2. S16 — read-back verification oracle (`internetMessageId` poll → VERIFIED_SUCCESS | UNKNOWN → HOLD). First `VERIFIED_SUCCESS` only here.
3. S17 — local-first (kill the sign-in wall); S34 — universal action-trace (a queryable admission record; none today).

## Wired this slice (S14)
- Real-Electron e2e harness (`e2e/mailSend.e2e.cjs`) drives the full governed loop in a launched app against a mock Graph.
- Compile-gated e2e seed (`src/main/e2e/e2eSeed.ts`) — fake principal + governed account + global-fetch mock; double-gated, structurally absent from release (`verify-e2e-strip.sh` PASS); `-e2e` version/title stamp.
- Generator refined: dispatch vs compose (drafting no longer hijacked); assistant asks on ambiguity.

## Honest status
Everything is **TEST-VERIFIED EXCEPT the S15/S16 chain, which is LIVE** — one real governed M365 send (provider-side observed) + its read-back VERIFIED_SUCCESS with captured internetMessageId. Destination receipt remains NOT GOVERNED; no other external effect exists. Nothing executes without human confirmation. Backend down/empty; builds unsigned; NOT CERTIFIED (13C). Mirror of CLAUDE §1 — that section is authoritative.
