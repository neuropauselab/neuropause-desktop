# NP_STATE.md — operational state (read after CLAUDE.md, before work)
Living, TRACKED working doc (committed each slice; excluded from the freeze source spec — see DECISIONS.md D-5). Mirror of CLAUDE.md §1 with operational detail.

## Now
- HEAD `60ac0e1` · FREEZE INTACT (`BASELINE-616c4a488937`) · branch `cert/data-import-cst-integration`.
- **🧠 L6 LIVE BRAIN — S1 (state), S2 (context/provenance), S3 (reasoning) ALL LANDED — ⛔ NOW AT THE L6-S4 HARD STOP.** S2 `liveBrain/brainContext.ts` `assembleBrainContext` → BrainContext of atomic PROVENANCED FACTS (per-field {layer, evidence object}); provenance-completeness + traceability pinned; coherent with S1 (atomic KNOWN vs synthesized CONFLICTING); `brainContext.test.ts` (7). S3 `liveBrain/brainReasoning.ts` `reason(state, context, previous?)` → BrainReasoning (what-is-happening/why/what-changed/what-is-missing/known/unknown/could/should-not), every statement cites its evidence; **REASONS, never PROPOSES** — NO-PROPOSAL invariant pinned (no proposedAction/requiredAuthority/execute/grant field; `could` only points at S4); what-changed UNKNOWN without a prior (never assumed stable); `brainReasoning.test.ts` (8). All deterministic (D-14), zero-runtime-import. Full main **845 files / 8895 passed / 3 skipped**; typecheck+lint clean; INTACT each slice. **S4 REVIEW PACKAGE (`L6-S4-REVIEW-PACKAGE-CHECKLIST.md`):** item 2 (S2+S3 summaries) ✅ READY; item 1 (independent 12-lens fleet audit) AWAITING QUIET WINDOW — mechanism = declared QUIET WINDOW per D-15; items 3 (proposal-schema worked example), 4 (L5 operational-bridge status+plan — NOT BUILT, it is proposal-adjacent so behind the gate), 5 (zero-authority extended to S4) assembled at the gate. **HARD STOP: no proposal code until the operator reviews the package. Separate HARD STOP before S5 (executions).**
- **🧠 L6 LIVE BRAIN OPENED (staged, operator-approved 19 Aug) — SLICE 1 STATE MODEL LANDED.** `liveBrain/liveBrainState.ts` `composeLiveBrainState(inputs)` → ONE truthful operational state as a PURE JOIN over the real L1–L5 read-models + S34a ActionRecord; five-valued uncertainty first-class per section (KNOWN/UNKNOWN/UNAVAILABLE/CONFLICTING/VERIFIED); UNKNOWN never "probably okay"; CONFLICTING detected+surfaced (scope · routed-but-verified-failed · env-NEED-vs-routed), both claims kept, never auto-reconciled; risk UNAVAILABLE (no model, not fabricated). All 5 pins green (pure join/no store · fail-closed scope · provenance · zero-runtime-import · conflicting-not-reconciled); `liveBrainState.test.ts` (11) over REAL read-models. **CLAUDE.md §2 #13 canonized** ("The Brain proposes; it never reaches.") + **DECISIONS D-14 deterministic-first** (no LLM feeds any L6 slice until Phase E). **Substrate grounded by DIRECT READ** — the 12-agent fleet audit was aborted by intervening messages (background agents share the session abort signal); 6 claims CONFIRMED, 1 CORRECTED (`actionRecord.query` async/disk → `ActionRecord[]` injected), folded into evidence (`…L6-S1-SUBSTRATE-GROUNDING-AND-REVIEW.md` + `…L6-LIVE-BRAIN-SLICE-1-STATE-MODEL-EVIDENCE.md`). Non-frozen (no frozen surface touched); full main **843 files / 8879 passed / 3 skipped**; typecheck+lint clean. **Staged control points:** S2/S3 pre-approved to continue on this clean landing; **HARD STOP before S4 (proposals)** and a **separate HARD STOP before S5 (executions)**. **S15 containment COMPLETE (19 Aug):** app deleted → consent revoked · registration deleted · profile rm -rf'd (latch store gone → no retry).
- **L2/L3 LIVE WIRING LANDED** (non-frozen): `environmentModel/liveSources.ts` `liveEnvironmentSources` builds L2's `probe` from the REAL L4 `CapabilityGraphSnapshot` (route→HAVE · not-governed gap→NEED · missing/unknown→UNKNOWN · !scopeResolved→UNAVAILABLE) + REAL L1 `workspaceDomain` rollup (present+records→HAVE · empty→NEED · unavailable/absent→UNAVAILABLE) — proven against real `composeCapabilityGraph`(real `mutationAssuranceFor`) + real `composeWorkspaceDomain`/`toWorkspaceDomainField`; local-mode honesty pinned. `environmentDiscovery/liveSources.ts` `liveDiscoverySources` binds L3 authority to cheap CONSENT facts (workspace resolved + connected account) decided WITHOUT reading the catalog, and collection to the REAL `CapabilityDiscoveryService.catalog()` probe run only under granted authority — **never-a-silent-scan proven with a catalog call-spy** (never invoked without authority) + bound to the real service's fail-closed empty catalog. Honest boundary recorded: per-capability discovery-consent surface is a future increment (not invented). Both adapters type-only imports (read-only-adapter invariant pinned); cores' zero-runtime-import unchanged. `environmentModel/liveSources.test.ts` (5) + `environmentDiscovery/liveSources.test.ts` (5); full main **842 files / 8868 passed / 3 skipped**; typecheck + lint clean. Evidence: `…ENVIRONMENT-MODEL-DISCOVERY-LIVE-WIRING-EVIDENCE.md`. **Next:** production call-site (compose real L4 graph + L1 rollup + catalog under active scope, feed the live sources; close L3→L2→L5 loop); renderer surface would touch frozen `packages/shared`→FG gate. **L6 entry condition remains fully met** (L1–L5 each ≥1 closed slice) — re-presentation is a human review gate.
- **OS-track (latest):** L1 Slice-2 CLOSED (FG-8 `workspaceDomain` rollup). **L4 Capability Graph — Slice-1 + LIVE WIRING LANDED**: `capabilityGraph/liveSources.ts` binds `composeCapabilityGraph` to the REAL `mutationAssuranceFor` predicate + BRAIN-1 draft lane + the one certified workflow (`propose→governedSend`); five fields re-proven (`liveSources.test.ts`, 5); proposal-only invariant extended. Honest finding: `mutationAssuranceFor` is PER-CONNECTOR → microsoft-entra cohorts read governed-certified → MISSING (no lane/workflow), NOT NOT_GOVERNED (Slice-1 assumption corrected); NOT_GOVERNED proven with a non-certified connector; per-capability distinction is S23. **L5 Purpose Engine — Slice-1 LANDED**: `purposeEngine/purposeEngine.ts` `evaluatePurpose` → pure nine-state `PurposeEvaluation` (UNKNOWN→STATED→RECOGNIZED→ROUTED→PERMITTED→CONSENT_READY + off-ramps NEEDS_CLARIFICATION/UNSUPPORTED/DENIED), halts at first missing rung, UNKNOWN stays UNKNOWN, proposals-only, zero-runtime-import invariant pinned (`purposeEngine.test.ts`, 13). **L2 Environment Model — Slice-1 LANDED**: `environmentModel/environmentModel.ts` `composeEnvironmentModel` → pure PURPOSE-BOUND four-state model (HAVE/NEED/UNKNOWN/UNAVAILABLE); UNKNOWN never silently becomes HAVE; purpose-bound proven by a probe call-spy; zero-runtime-import (`environmentModel.test.ts`, 9). **L3 Environment Discovery — Slice-1 LANDED**: `environmentDiscovery/environmentDiscovery.ts` `runDiscovery` → purpose-bound pipeline PURPOSE→REQUEST→MINIMUM-REQUIRED→AUTHORITY→COLLECTION→CLASSIFICATION→EVIDENCE; **NEVER A SILENT SCAN** (collect() gated on granted authority, call-spy proves denied/unknown never collected); `discoveryRequestFor(model)` derives minimum-required from L2's NEED+UNKNOWN gaps (proven against a real L2 model); DISCOVER≠RECOMMEND≠BUILD; zero-runtime-import type-only coupling to L2 (`environmentDiscovery.test.ts`, 10). All four slices non-frozen, no FG gate. Full main **840 files / 8858 passed / 3 skipped**; typecheck + lint clean. Evidence: `…CAPABILITY-GRAPH-LIVE-WIRING`, `…PURPOSE-ENGINE-SLICE-1`, `…ENVIRONMENT-MODEL-SLICE-1`, `…ENVIRONMENT-DISCOVERY-SLICE-1`. **🎯 L6 ENTRY CONDITION FULLY MET — L1, L2, L3, L4, L5 each now have ≥1 CLOSED slice.** Per §5, L6 (Live Brain) may now be RE-PRESENTED with its five fields refreshed against the real substrate (approved-in-principle; entry was deferred until this condition — a human review gate, not autonomous).
- Suites (RUN against BASELINE-43dfbe3ff6f7): full main **8724/3 skipped** (823) · real-Electron e2e **13/13** (guard inert in mock mode) · guard+mode pins **16** · typecheck + lint clean · `verify-e2e-strip.sh` PASS (seed + guard absent from release).
- Landed: Slices 1–14 + **S15 EXECUTED**. S14 = real-Electron governed-loop e2e (mock Graph). S15 = first REAL send ATTEMPT at the operator's keyboard: **AUTHORIZED ✓ · SUBMITTED ✓ (Graph 202/ACKNOWLEDGED) · EXTERNALLY OBSERVED = PENDING** (automated read-back blocked by macOS Keychain, F-3). One email to the operator's own address; latch spent. FG-4 also fired live on a non-allowlisted attempt (negative proof). Profile-isolation guard landed (`996958d`, `--user-data-dir` required). Evidence: `…SLICE-15-FIRST-REAL-SEND-EVIDENCE.md`.
- **Slice 16 — DONE: FIRST VERIFIED_SUCCESS in product history.** Real in-session run (2026-08-18T13:25:54Z): TERMINAL=VERIFIED_SUCCESS, internetMessageId `<PN2P287MB1597…@…PROD.OUTLOOK.COM>`, bounce=none, attempts=1, corroborated Sent Items match. `verifyEffect` (17 pins incl. fault injection) + `m365ReadBack` + `s16VerifyRun`. No FG-5 (D-10); verify-e2e-strip PASS. Artifacts in `certification/s15-artifacts/`.
- **Slice 17 — FG-6 LANDED + renderer wall-kill (in progress; e2e unconfirmed).** Honest `local` AuthStatus + device-local principal via **FG-6** (token honored; INTACT #1 `BASELINE-3f19fb707fa0` → INTACT #2 `BASELINE-43238f789694`; then re-record at renderer `BASELINE-1dbef0d5bbdf`). `resolveGovernedActor` (`local:<id>` self-disclosing/forgery-denied/no-strip, S33 edge pinned) + `sessionEmailFor` (`local-<id>@device.invalid`, invalid-by-rule) + `localPrincipalStore` (INSTALL_GLOBAL, backup-registered). App.tsx `local` branch → full shell + `LocalModeBanner`. Suites: main **8766/3** (828) · UI **257** (35) · typecheck+lint clean · verify-e2e-strip PASS. DECISIONS D-11/D-12; CLAUDE §4 AuthStatus-exhaustiveness standing rule. **`local-mode.spec` GREEN 5/5** + **graceful cloud-absence LANDED** (`useIsLocalMode`/`CloudUnavailableLocal`/`LocalModeConnectProvider`; Org/Store/Devices/Billing show honest absence, not the "Sign in to manage…" red banner; semantic/livesync/license already graceful). UI 259 · typecheck+lint clean. **S17 CLOSED** (DoD walked in evidence). **F-S17-1** → S39.
- **BRAIN-1 CLOSED (①②③④, non-frozen, no FG gate; TEST-VERIFIED).** + CLAUDE §3 standing rule: any renderer-touching commit runs the FULL main suite (never the ui glob alone). §5 amendment draft revised to v2 (five-field acceptance standard per layer + Environment-Intelligence rules + canonical preamble) — awaiting approval. ① hostile-adapter CI gate (draft model gains no authority). ② `ai/brain/mailDraftGateway.ts` (model → zod → 1 repair → referenceDrafter fallback). ③ assistantService via `servingDraftMailer()` (reference-only). ④ `draftEval.ts` harness + eval report (honest zero-model baseline, 0 candidates) + DECISIONS D-13 + badge (names a model only when one serves) + LiteLLM reserved. Fixed a latent S17 `cssTokens` failure (LocalModeBanner `--surface`). Full main **8784/3** · UI **259** · typecheck+lint clean. Draft lane serves `referenceDrafter` until eval clears + operator go.
- **S34a — Queryable Action Record CLOSED (FG-5; TEST-VERIFIED).** `connectors/actionRecord.ts` + ONE gated observer line in frozen `connectors/index.ts` (INTACT #1 `322115041eeb` → INTACT #2 `8f0f1e137d3f`). Best-effort observer, never blocks the send; chain + fingerprints + `recordVerification`; tenant-isolated `query`. 13 tests (all 8 closing proofs + query proof + observer invariant). Full main **8796/3** · UI **259** · verify-e2e-strip PASS. Scope-fenced.
- **S19 — Counter truth CORE CLOSED (F-5 in logic; TEST-VERIFIED, non-frozen).** Definition pass first; five states (`connectors/m365WriteStates.ts`) derived from the single S34a `ActionRecord` store (no parallel counting). F-5 reproduced (old counter 0/never) + made truthful (7 tests). **Display landed via FG-7** (INTACT #1 58024ee849b0 → INTACT #2 959735ab2a6a): additive `ConnectorSyncSnapshot.writeStates`, unified/sync join, panel retires the old counter. **S19 CLOSED — F-5 end-to-end.** Full main 8809/3 · UI 262.
- **§5 amendment APPROVED per-layer** (L1–L5 into §5; L6 approved-in-principle, entry deferred). **FG-7 presented** (ConnectorSyncSnapshot five-state display — holds for the operator's token). **Workspace Foundation (L1) Slice 1 observable object LANDED** (`enterprise/workspaceFoundation/domainAggregate.ts`, READ/aggregate-only tenant-scoped domain snapshot; 5 tests; non-frozen). Honesty line fixed (S15/S16 LIVE). **Next:** WF live-wiring increment (registry + existing channel, non-frozen); FG-7 on token. **S15 containment COMPLETE (2026-08-19, operator keyboard):** enterprise application deleted → consent revoked · app registration deleted · S15 profile `rm -rf`'d (evidence copied out first; latch store gone → no retry possible). Recorded in `…SLICE-15-FIRST-REAL-SEND-EVIDENCE.md` §Containment.

## Change-control trail (S14)
```
9f4d7dd  freeze re-record — foundation INTACT
8c72836  alive(s14): e2e seed + mock-Graph seam (structurally absent) + verify-e2e-strip.sh
caac9c0  alive(s14): dispatch-vs-compose intent + assistant asks on ambiguity
0be757b  alive(s14): real-Electron governed-loop e2e (mock Graph) — S14-A/B/C green
f254f2d  fix(s14): seed uses authService.setStatus (renderer leaves the sign-in wall)
ef9c9f0  freeze re-record — INTACT (BASELINE-9f4d36abed4e)
```

## Next 3 steps
1. **S15 — first REAL email ⛔ human at keyboard.** Runbook prepared. HARD STOP before real credentials / OAuth / real send. The compiled-in recipient-allowlist guard lands here (gated if it must sit in a frozen send path).
2. S16 — read-back verification oracle (`internetMessageId` poll → VERIFIED_SUCCESS | UNKNOWN → HOLD). First `VERIFIED_SUCCESS` only here.
3. S17 — local-first (kill the sign-in wall); S34 — universal action-trace (a queryable admission record; none today).

## Wired this slice (S14)
- Real-Electron e2e harness (`e2e/mailSend.e2e.cjs`) drives the full governed loop in a launched app against a mock Graph.
- Compile-gated e2e seed (`src/main/e2e/e2eSeed.ts`) — fake principal + governed account + global-fetch mock; double-gated, structurally absent from release (`verify-e2e-strip.sh` PASS); `-e2e` version/title stamp.
- Generator refined: dispatch vs compose (drafting no longer hijacked); assistant asks on ambiguity.

## Honest status
Everything is **TEST-VERIFIED EXCEPT the S15/S16 chain, which is LIVE** — one real governed M365 send (provider-side observed) + its read-back VERIFIED_SUCCESS with captured internetMessageId. Destination receipt remains NOT GOVERNED; no other external effect exists. Nothing executes without human confirmation. Backend down/empty; builds unsigned; NOT CERTIFIED (13C). Mirror of CLAUDE §1 — that section is authoritative.
