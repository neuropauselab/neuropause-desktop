/**
 * Timeline tab route (Mobile M1-11). Thin binding; screen lives in src/screens.
 */
import { TimelineScreen } from '../src/screens/TimelineScreen';

export default function TimelineRoute(): JSX.Element {
  return <TimelineScreen />;
}
