/**
 * Search tab route (Mobile M1-11). Thin binding; screen lives in src/screens.
 */
import { SearchScreen } from '../src/screens/SearchScreen';

export default function SearchRoute(): JSX.Element {
  return <SearchScreen />;
}
