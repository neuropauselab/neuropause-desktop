/**
 * Industry tab route (IP-11). Thin binding for expo-router; the screen lives in
 * src/screens.
 */
import { IndustryScreen } from '../src/screens/IndustryScreen';

export default function IndustryRoute(): JSX.Element {
  return <IndustryScreen />;
}
