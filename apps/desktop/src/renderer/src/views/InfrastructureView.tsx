import { CloudPlatformCenterRoot } from '@renderer/infrastructure/InfrastructurePage';

/** The Infrastructure section — the Cloud Platform Center (P6). */
export function InfrastructureView(): JSX.Element {
  return <CloudPlatformCenterRoot />;
}
