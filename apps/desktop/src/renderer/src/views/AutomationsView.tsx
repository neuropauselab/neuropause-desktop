import { AutomationBuilder } from './AutomationBuilder';

/** Module 9 — the production Automation Builder (V4.6). */
export function AutomationsView(): JSX.Element {
  return <AutomationBuilder />;
}
