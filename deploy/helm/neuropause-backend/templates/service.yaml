apiVersion: v1
kind: Service
metadata:
  name: {{ include "neuropause-backend.fullname" . }}
  labels:
    {{- include "neuropause-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "neuropause-backend.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
