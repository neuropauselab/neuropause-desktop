apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "neuropause-backend.fullname" . }}-config
  labels:
    {{- include "neuropause-backend.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
